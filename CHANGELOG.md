# NihaoC 变更日志 | Changelog

版本号遵循 [Semantic Versioning](https://semver.org)。本文件记录 NihaoC 编译器（`ncc/`）的里程碑与版本变更。

## [v1.0.2] — 2026-09-19（待 tag）

1.0 冻结线的规范合规与代码卫生版本，无新增语言特性（`is _` 通配符属补全文档已承诺的既有语法，`structof` / `unionof` / `holdof` / `bitoffsetof` / `len` 属补回文档已承诺却缺失的内置函数，§5.1 的指针后缀链与切片写法属补回文档已承诺却编译不过的代码生成（PA-25 / PA-26），均非新特性）；三处语法收敛为**移除冗余记号**——`is` 的 `=>` 单语句形式（PA-13）、`?=` 安全赋值（PA-20）、`?.` / `?(` 安全解引用（PA-21），检查语义统一由 `=` 与 `.()` 自身承担。

- **`is` 移除 `=>` 单语句形式（PA-13，用户可见语法变更）**：`is <pattern> => <statement>` 全线下线，`is` 只保留块形式 `is <pattern> { ... }`。C 后端 `parser.c` 的 `parse_is_stmt` 删除 `TOK_FAT_ARROW` 分支（改报 `expected '{' after 'is' pattern`）；IR 前端 `irparse.c` 同分支删除（双前端与 BNF v2.2 一致，错误路径吞 token 防死循环）；`tests/pos/ir_is.nc` 清理 `=>` 用例。`=>` 仍由 lexer 识别为 `TOK_FAT_ARROW`（词法保留，语法不使用），`token.h` 注释同步。
- **`codegen.c` 死代码全链清理**：删除早期直出 C 的后端 `ncc/codegen.c`（507 行，已由 `cgen.c` + `parser.c` 管线取代）及其调用链——`linker.c` 相关 120 行、`ncc.h` 84 行声明与枚举、`ncc.c` 4 行后端注册；`ncc/xmake.lua` 的 `add_files` 去掉 `codegen.c`。
- **构建修复（用户可见）**：`xmake.lua` PATH 探测的 `gmatch` 模式误把盘符冒号当分隔符切开（`D:\...` 被截断）导致编译器探测失败；同时修正含空格 TCC 安装目录的参数引号处理。
- **BNF 收敛入文档**：v2.1 记录指针语法收敛（一元 `*` 解引用移除，解引用统一 `.()` / `.(T)` / `->`）；v2.2 重写 `<pattern>`（通配符 / 整数 / 负整数 / 闭区间 `lo..hi` / 枚举变体 / 可见性枚举 / 结构体解构与 ADT 变体解构（预留）/ 标识符），`<is-stmt>` 从 `<statement>` 移除、`while` 体内 `is-clause` 与 `do` 不支持 `is` 口径对齐，中英 §6.1/§13.3 与语法元素表同步。
- **TODO 分层拆分**：`TODO.md` 拆为 `ROADMAP.md`（跨分支里程碑与通用待办）+ `TODO-PA.md`（1.0 冻结线专属）+ `TODO-PB.md`（2.0 IR 线专属），并建立共享文档跨分支同步规则。
- **文档-实现全量一致性核对**：PB-27 子项状态回填、PB-29 `ret_vis` 检查条件表述修正（`ret_vis > VIS_VAR` 恒假 → 显式 `∈ {CONST, FLOW, STATIC}`）、ROADMAP 架构图清除已删除的 `ir_to_native.c` 引用、VERSIONING_ROADMAP 阶段 2 重复条目去重、`ncc.h` 后端注释与 `set_version` 对齐；中英 §12.2 的「参数前缀未强制」实现状态注改写为版本口径（1.0 不实现 / 2.0 线 PB-26 已实现），并撤除用户文档中的内联源码行号——行号统一由 `docs/IMPLEMENTATION_STATUS.md` 单点维护。
- **`is _` 通配符补全（规范合规）**：1.0 线 A 后端 `parse_is_stmt` 原先把 `_` 当普通标识符输出到 C（tcc 报 `'_' undeclared`），现补恒匹配分支（生成 `if (1)`，与 2.0 线同源实现）；IR 前端 `irparse.c` 同步补 `_` 分支（不发比较与 JZ，块直接执行）。`tests/pos/pattern.nc` 增加通配符用例并更新 `.expect`，c/native/ir-c/ir-native 四后端输出一致。（PA-15）
- **`is` 循环体外守卫（规范合规，用户可见报错变更）**：BNF §6 规定 `is` 仅配合 `while` 循环体，但 1.0 线 A 后端把 `TOK_IS` 挂在通用语句分支（旧位置），循环体外写 `is` 会被前端接受、错误延迟到 C 编译期才由 tcc 报 `'__is_val' undeclared`。现加 `while_depth` 计数守卫（`TOK_WHILE` 体内递增/退出时递减），循环体外（含 `do` 体、函数顶层、模块层）的 `is` 由前端即时报 `'is' pattern match only valid inside while loop body`，与 IR 前端既有守卫（`is_val_vreg < 0`）口径统一。新增 `tests/err/is_outside_while.nc` 覆盖。（PA-18）
- **`do` 体内 `is` 在 IR 前端拒绝（规范合规）**：IR 前端 `TOK_WHILE || TOK_DO` 分支此前无条件把条件值写入 `is_val_vreg`（守卫变量 `is_do` 算了却未使用），导致 `do cond { is pat { ... } }` 在 ir-c / ir-native 下被接受，与 BNF §6 / 中英 §6.1「`do` 不支持 `is`」相反（A 后端经 PA-18 已拒绝）。现 `do` 分支置 `is_val_vreg = -1`，体内 `is` 报 `ir: 'is' pattern match only valid inside while loop body`（文案去掉 `while/do` 的 `/do`），与 PB-27.1 对齐。新增 `tests/err/is_in_do_body.nc`。测试框架同步收紧：err 用例改为仅 `IR_ERR_SKIP` 列出的 M2 四条（`m2a`~`m2d`）在 IR 后端跳过，其余 err 用例四后端均执行。同项续做：A 后端进入 `do` 体前把 `while_depth` 清零、退出恢复，使嵌套在 `while` 内的 `do` 也拒绝 `is`（原会静默匹配外层条件值），与 IR 前端对齐；`tests/err/is_in_do_body.nc` 增加该嵌套情形。**文档理由句修正**：BNF §6 与中英 §6.1 原写「`do` 不支持 `is`，因为 `do` 先执行块再判断条件」——与实现相反（本语言 `do cond { ... }` 与 `while` 同为前测循环，A 后端生成 `while (cond) { body }`）。现改为陈述为规范规定（`is` 只绑定 `while`，`do` 是否纳入留待 2.0 定案），不再使用错误的因果论证。（PA-19）
- **`?=` 安全赋值记号从语法移除（PA-20，用户可见语法变更）**：文档曾把 `?=` 列为"安全赋值操作符（带指针检查）"，但 1.0 线实现里 `?=` 与 `=` 走完全同一条分支（声明、语句窥探、赋值三处共用），没有任何额外检查——因为普通 `=` 本身就无条件执行 §12.1 可见性兼容性检查。故 `?=` 是**纯冗余记号**且制造"只有 `?=` 才安全"的误解。现从语法移除：`parser.c` 的 `parse_assign` 不再与 `=` 共用分支，`case TOK_SAFE_ASSIGN` 即时报错 `'?=' is not part of the grammar; use '=' (every assignment is checked for visibility compatibility)`；`is_expr_continuer` 与语句窥探分支同样只认 `TOK_ASSIGN`。词法保留 `TOK_SAFE_ASSIGN`（lexer 仍识别，与 `=>` 同处理）。BNF 升级 **v2.3**：§1.4 运算符与 `<assign-op>` 去掉 `"?="`、说明改为词法保留、附录删除"安全赋值"行；中英 §5.1 多级指针示例与 §12 设计说明同步改写为"每次赋值都做可见性检查"，两份语法元素表移除 `?=` 行并给 `=` 补注。新增 `tests/err/safe_assign_removed.nc`（`.expect` 只取 `'?='` 子串，同时匹配 A 后端的专门文案与 IR 前端的通用 unexpected token）。IR 前端自始无 `?=` 分支，行为即为拒绝，无需改动。**PB 线仍接受 `?=`，待回灌**（见 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」）。
- **`?.` / `?(` 安全解引用记号从语法移除，检查落入 `.()`（PA-21，用户可见语法变更 + 检查补全）**：文档 §14.1 承诺"用 `?.` 做安全解引用，编译器结合可见性检查与边界检查"，但实现里 `?.` 只是 `.(` 的别名（`parse_postfix` 两 token 同路），`?(` 甚至从未成为 token（词法切成 `?` + `(`），所谓"安全"两种写法都没有。按"功能都加、语法收敛"决策：移除附加记号，把承诺的检查真正做进 `.()`。① A 后端 `parse_postfix` 循环加 `TOK_SAFE_DOT` 显式拒绝分支，报 `'?.' is not part of the grammar; use '.()' (dereference performs the visibility and bounds checks)`；`is_expr_continuer` 与解引用窥探去掉 `TOK_SAFE_DOT`。IR 前端 `irparse.c` 同位置加同文案分支（`ir: '?.' is not part of the grammar; ...`），两线口径一致。② `parse_deref_chain` 补编译期越界检查：读侧已有 `vis_check_usable`、写穿侧由赋值语句的 `vis_check_writable` 覆盖，新增宽度检查——`.(T)` 读取的 `sizeof(T)` 若超过该指针**静态已知**所指对象字节数即报错。字节数来源：`malloc(T)` / `malloc(T,n)`（n 为字面量）初值记入新的 `Symbol.pointee_bytes`，或 `&x` 取址由 `sym->type->ref->size` 推得；对指针本身重新赋值时清零（目标宽度不再静态可知，保守不检查，`lhs_was_deref` 区分 `p.(T) = v` 与 `p = v`）。③ 运行期 `NULL` 检查不属本项：非空由静态规则保证（指针声明必须初始化、不允许空指针，§5.1），`malloc` 返回值的运行期检查留待 2.0，文档按此改写并注明现状。BNF v2.3 同步：§1.4 运算符终端与 `<postfix-op>` 去掉 `"?." <identifier>` 与 `"?(" [ <type-name> ] ")"`，`.()` 说明改为承担可见性 + 越界检查，附录改列 `解引用 .() / .(T)`。中英 §5.1 示例（`ptr?.(i64)` → `ptr.(i64)` 并注越界报错）、§14.1「安全解引用与访问」整节改写，中英语法元素表移除 `?.` / `?(` 行、`.()` / `.(type)` 补注检查语义，英文运算符清单去掉 `?. ?(`。新增 `tests/err/safe_dot_removed.nc`、`tests/err/deref_bounds.nc`；后者依赖 `.(T)`，IR 前端仅支持裸 `p.()`，故一并加入 `IR_ERR_SKIP`。struct/array/宽分配探针确认无误报。**PB 线仍把 `?.` 当 `.(` 别名、`.(T)` 与宽度检查均无，待回灌**（见 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」）。
- **从属查询与位域偏移内置函数补实现（PA-22，2026-09-20，规范承诺落地 + 签名定稿）**：中英 §2.3 早已列出 `structof` / `unionof` / `holdof` / `bitoffsetof`，A 后端却完全没有这些分支——`parse_primary` 只把 `sizeof`/`typeof`/`alignof`/`offsetof`/`visof` 分派给 `parse_builtin_kw`，四个关键字落入表达式默认分支报 unexpected token。按"补漏、中英保持一致、补实现到 A 后端"决策执行，并进一步把签名定稿为**三参**（原二参 `structof(T, expr)` 拿不到成员偏移，无法由成员地址反推首地址）：① `structof(T, m, p)` / `unionof(T, m, p)` / `holdof(T, m, p)` 生成 `((void*)((char*)(p) - offsetof(T, m)))`（container_of 语义），并给出诊断——`T` 非聚合体、`structof` 用于 `union`、`unionof` 用于 `struct`、`m` 不是 `T` 的成员，均即时报错；`holdof` 对 `struct`/`union` 通用。② `bitoffsetof(T, m)` 语义定为**声明序位偏移**：以前置最近非位域成员为锚点，用 C 自身 `offsetof`/`sizeof` 求存储单元起点（对齐以基类型 `sizeof` 近似，基本整型 `align == size`），再加整单元数与单元内位数；目标非位域、基类型宽度未知、同组存储类型不一致时报错。③ BNF 升级 **v2.4**：`<builtin-call>` 三条从属查询改为三参并补 `bitoffsetof` 语义注释；中英 §2.3 同步定稿签名与"反推所属聚合体首地址"表述、加版本口径注（1.0 线由 A 后端提供，`ir-*` 属 2.0 布局待做），中英 §5.2 的 `structof` 示例改为可编译写法。两份语法元素表的四个关键字行补签名说明。④ 新增 `tests/pos/ownerof.nc`（struct/union 反推 + 位域 `8/12/64` 三值断言）与 `tests/err/structof_bad_member.nc`；IR 前端不改动（2.0 布局待做），pos 用例不入 `IR_SUBSET` 自动跳过、err 用例加入 `IR_ERR_SKIP`。**探针另发现两项待决缺口**：`alignof(T)` 生成 `_Alignof`，tcc 0.9.27 不提供该符号导致 `c` 后端链接失败；中英 §14.1 等处以逗号分隔结构体成员，而实现只接受空白分隔。均登记 `TODO-PA.md`（PA-24 / PA-23）待决策。**PB 线未接入这四个关键字，实现待回灌**。
- **指针后缀链与切片补实现（PA-25，2026-09-20，规范承诺落地）**：中英 §5.1.2 / §5.1.3 通篇使用 `p.().()`、`arryptr.(char[9])[0]`、`arrybuf char[8] = arryptr.(char[9])[0..7]`、`dptrarry1 void[3] = malloc(void[3])` 等写法，而 A 后端把解引用锁在独立的 `parse_deref_chain` 里——`.(T)` 之后不能再接下标/成员/切片，空 `[]`、切片读、切片写、数组初始化退化全部无法编译。按"补实现，不降级文档"决策执行：① 删除 `parse_deref_chain`，解引用改为**通用 postfix 链**上的 `deref_step` / `deref_prefix`（每个算子把已生成的前缀 C 文本整体包裹后继续），数组类型化解引用生成合法的 `T (*)(N)` 转换（`p.(char[9])` → `*(char (*)[9])(p)`）。② 空下标 `p[]` 定义为省略类型的 `p.()`（一层解引用），§5.1.1 的三级指针链 `p.().().(i32)` 遂可写作 `p[][].(i32)`。③ 切片读 `p[a..b]` 生成"指向第 a 号元素的指针"（C 无数组切片类型，长度不进入值本身），`[..b]` 省略起点等价 `[0..b]`；赋给固定数组 `T[n] x = p[a..b]` 时按**声明容量**逐元素复制（`memcpy`），非别名。④ 切片写 `p[a..b] = {v0, v1, …}` 逐元素写回（逗号表达式），右值非值列表时报错。⑤ `T[n] x = malloc(T[n])` 声明退化为指针（仅省略最前维度：`void[4][5]` → `void* (*x)[5]`），`malloc` 字节数按元素个数乘算，供 `.(T)` 宽度检查使用。⑥ 通用 `void` 指针（`varptr void = &x`）元素宽度静态未知，裸下标/切片改为前端即时报错并给出修复写法：`cannot subscript the generic 'void' pointer 'x'; write x.(T)[i] to fix the element type first`（文档 §5.1.2 相应把 `arryptr[0] = 0` 改写为 `arryptr.(char[9])[0] = 0`）。⑦ 顺带修复多维数组声明 `i32[2][3]` 只输出最后一维（`c_type_suffix` 现按源码顺序输出全部维度）。文档：BNF 升 **v2.5**（`<postfix-op>` 的 `[]` / `.()` 语义注释 + 通用 void 指针限制）、中英 §5.1.2 / §5.1.3 示例全部改写为可编译形式并加"数组指针与切片""`T[n]` 配 `malloc` 退化"两条定案说明、§5.1.3 原先错误的 `.(int64)` 报错注释改为实际的跳过检查口径（指针数组槽位宽度静态不可知）。测试：新增 `tests/pos/deref_slice.nc`（8 项断言，覆盖上述全部形态）与 `tests/err/void_subscript.nc`（入 `IR_ERR_SKIP`）。IR 前端不改动（属 2.0 布局/代码生成待做），pos 用例不入 `IR_SUBSET` 自动跳过。**PB 线未接入，代码生成待回灌**；§5.1.4 的字符串右值切片赋值（`stptr.(char[9])[0..8] = "xiaoming"`）登记为 PA-26。
- **`len(x)` 内置函数补实现（PA-27，2026-09-20，规范承诺落地）**：中英 §2.3 自始把 `len(x)` 列为内置查询（数组=声明容量 / 动态字符串=字面量长度 / 切片=边界差），A 后端却完全没有这个分支——`len` 被当成普通标识符，符号表里没有它就直接报错。按"三类全补"决策实现：① 在 `Symbol` 上新增逻辑长度记录（`len_known` / `logical_len`），于**声明期**登记：数组取各维乘积（多维 `len` 即元素总数），`char[] s = "..."` 这类未定长数组/字符串取字面量长度，类型推断的切片声明取 `hi-lo`。② `len` 接入 `parse_primary` 的标识符内置函数表（实参必须是标识符）；实参的逻辑长度**静态不可知**时前端即时报 `len: logical length of 'x' is not statically known`，而不是返回一个过期或零值。③ 对变量整体重新赋值（`s = ...` 而非 `s[i] = ...`）时撤销登记，避免 `len` 返回与当前值无关的旧长度。④ 类型推断的切片声明 `s = p[a..b]` 改为**指针视图**代码生成（`E *s = &p[a]`，`s.()` 即第 a 号元素），修掉 PA-25 遗留缺陷——旧实现按右界分配并 `memcpy`，会读到切片右界之外；固定数组 `T[n] x = p[a..b]` 仍按声明容量复制。文档：BNF 升 **v2.6**（`len` 产生式注明三类取值与"多维为各维乘积"，并加版本口径）、中英 §2.3 的 `len` 条目定稿（数组=容量、动态字符串=字面量长度、切片=`hi-lo`，静态不可知即报错；`ir-*` 后端三类取值已有但不可知时返回 0）、中英 §5.1.2 的切片定案注由"`b` 只作书写提示、不记录长度"改写为"上下界同为字面量时记录 `b-a` 供 `len()`"，并写明视图/复制之分。测试：新增 `tests/pos/len_builtin.nc`（8 项断言：数组、多维乘积、动态字符串、字面量、闭区间切片、左开区间、切片复制、整体重赋值后不受影响）与 `tests/err/len_unknown.nc`（入 `IR_ERR_SKIP`）。IR 前端不改动。**PB 线未接入 `len` 的报错口径与视图代码生成，实现待回灌**。
- **`else if` 在 IR 前端补支持（PA-28，2026-09-20，规范承诺落地）**：BNF `<if-stmt> ::= "if" <expr> <block> [ "else" ( <if-stmt> | <block> ) ]` 与中英 §6/§13 的示例一贯包含 `else if`，A 后端 `parse_if_stmt` 也支持，但 IR 前端在 `else` 之后无条件进入块解析，`else if` 在 ir-c / ir-native 下报 `expected '{', got 'if'`。修法：把 IR 前端的 if 语句抽成 `ir_if_stmt`，`else` 后遇 `if` 即递归自身，多级分支共用同一条 `JZ`/`JMP` 标签链（不新增 IR 指令，也不改变已有 `if`/`else` 代码生成）。新增 `tests/pos/elseif_chain.nc`（链中命中、中间命中且末尾无 `else`、全不命中、循环体内、`else` 普通块五种形态）并入 `IR_SUBSET`，四后端输出一致。文档：BNF v2.6 版本行追加记录该口径（产生式未变），`IMPLEMENTATION_STATUS.md` 新增「条件与分支（§6）」节并在「2.0 线（PB）差异」登记 PB `irparse.c` 仍缺该项。**PB 线待回灌**。
- **§7.3 调用方接收规则与 §12.1 传递矩阵收口（PA-29，2026-09-20，文档一致性定案，无代码变更）**：中英 §7.3 的接收表与 §12.1 矩阵互相矛盾——§7.3 规定 `flow` 返回值只能由 `flow` 接收并把 `const` 列入禁止项，而 §12.1 明确允许 `flow → const`（只读借用）与 `flow → var`（可变借用）；`const` 行的禁止列漏了 `var`（§12.1 禁止 `const → var`）；三行还把 `func` 当作可被禁止的"接收属性"列出，而 §12.1 已规定 `func` 不是变量存储期、不参与矩阵。按"以 §7.3 为准"决策收口：**返回值规则比赋值更严格是有意设计**——§12.1 约束的是同一作用域内已存在变量之间的赋值（源变量仍在，借用结束后解冻或由其 owner 释放），而返回值是临时量，用 `var`/`const` 接收 `flow` 返回值同时造成无人释放与悬空。文档改动：§7.3 三行禁止列改为纯存储期属性（`flow` 行 → `static`/`const`/`var`，`const` 行补 `var`）、新增"`func` 不能作接收属性"注、补"为什么本表比 §12.1 更严格"论证段与三条推论、示例增加 `const q void = create_buffer(1024)` 反例；§12.1 的 `func` 说明改为"本矩阵只描述同一作用域内变量之间的赋值"并回指 §7.3；两份文档同步加**实现状态注**（1.0 线不强制执行、2.0 线 PB-29 `ret_vis` 已实现、冻结线不移植；探针 `const q void = create_buf(4)` / `var r void = create_buf(4)` / `flow t void = get_ver()` 在 c/native 均静默编译并运行）。`IMPLEMENTATION_STATUS.md` 新增「调用方接收规则（§7.3）」三行表、§12.1 节标题限定为"仅限同一作用域内变量之间的赋值"、PB 差异行里不存在的「§12.3 后半」引用改为 §7.3。文档-only：门禁计数、代码与测试均不变。
- **结构体成员分隔符文档示例修正（PA-23，2026-09-20，文档-only）**：按"只改文档示例"决策，把中英 §7.4.1 与 §14.1 的 4 处 `Person struct { name char[], age i32 }` 改为空白分隔 `Person struct { name char[] age i32 }`。BNF `<type-decl>` 的成员列表 `{ <field-decl> }` 本就没有分隔符终端，双前端也一律拒绝逗号（A 后端 `expected member name, ','`、IR 前端 `ir: expected member name`），`ncc/tests` 与 `ncc/examples` 零处使用逗号——即 BNF 与实现一致，只有文档示例越界，故不改 BNF、不改实现。改后形态经 c / native 探针确认可编译可运行。**探针另发现两项 IR 前端（2.0 线）缺口**并登记进 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」：IR 前端拒绝任何数组类型结构体成员（`char[8]` 与 `char[]` 均报 `expected member name`）、IR 前端未内建 `print`（链接期 `undefined symbol 'print'`，IR 侧只有 `puts`）；两项均不影响 1.0 线。同项另发现 `print` 语义在规范中完全缺失（实现是 `printf` 直通，故 `print("[LOG] ", msg)` 只输出 `[LOG] `），登记为 PA-30 待决策。
- **`is` 多子句首个匹配即止（无 fallthrough）在 A 后端实现（PA-16，2026-09-20，用户可见语义变更）**：文档（中英 §6.1 / BNF §6 说明）一贯规定多个 `is-clause`「按源码顺序求值，首个匹配者执行（无 fallthrough）」，而实现为每个子句生成并列独立 `if`，条件重叠时多个子句都会执行（`is _` 恒匹配使该问题更易触发）。按"只修 A 后端，IR 留 2.0"决策执行：`while` 的代码生成在 `__is_val` 旁增加 `__is_matched` 标记（声明在 `for (;;)` 的外层块，嵌套 `while` 各自持有独立标记），每轮迭代在条件检查后清零；`parse_is_stmt` 的每个子句守卫改为 `if (!__is_matched && <pattern> && (__is_matched = 1))`——「此前未匹配 + 本模式命中」才进入块，进入即置位使后续子句全部短路；前子句不匹配时后续子句照常执行。置位写在条件中是因为块体由 `parse_statement` 整体输出，无法在其 `{` 之后插入语句。新增 `tests/pos/is_no_fallthrough.nc`（重叠的 `is 3` 与 `is 1..5` 只输出 `only three`，其余取值由后续子句接手，故同时覆盖每轮重置）；IR 前端按决策保持并列 `if`，该用例未列入 `IR_SUBSET`、在 ir-c / ir-native 自动跳过。文档：中英 §6.1 实现状态注改写（A 后端已实现、IR 后端属 2.0 待对齐）、BNF §6「匹配顺序」条补落地口径、`IMPLEMENTATION_STATUS.md` 该行转 ✅ 并把「2.0 线（PB）差异」行改为"1.0 已实现、PB 两前端待回灌"。门禁 c/native 各 **29P/0F/5S**、ir-c/ir-native 各 **6P/0F/24S**，0 FAIL，examples 6/7；既有单子句用例（`pattern.nc` / `ir_is.nc`）行为不变。**PB 线待回灌**（可直接移植 `__is_matched` 方案）。**属规范合规（补齐规范已承诺的语义），允许在冻结线执行**
- **`alignof(T)` 改由编译器编译期算对齐（PA-24，2026-09-20，用户可见缺陷修复）**：A 后端原先生成 C 的 `_Alignof(T)`，而内置的 libtcc 0.9.27 不提供该符号——`alignof` 在 `c` / `native` 后端一律链接期报 `undefined symbol '_Alignof'`，与中英 §2.3、BNF `<builtin-call>` 把它列为可用内置函数相矛盾（PA-22 探针发现）。按"ncc 编译期自行算对齐，输出字面量"定案：新增 `type_align()`（`type.c`，原型入 `ncc.h`）递归求对齐——数组/别名经 `ref` 取元素对齐、`struct`/`union` 取最宽非位域成员、`void` 依 §5.1 通用指针口径计 8、其余取 `CType.align` 并在其为 0 时回落 `type_default_align(kind)`（`parse_type` 在零初始化的栈上 `CType` 构造标量，不填 `align`）；`parser.c` 两处分派（关键字路径与标识符内置函数表路径）改为输出整数字面量，生成的 C 不再出现 `_Alignof`。PA-22 的 `bitoffsetof` 仍以基类型 `sizeof` 近似对齐，本项不回改。文档：中英 §2.3 的 `alignof` 条目补"编译期常量 + 三类取值口径 + `void` 计 8"、BNF `<builtin-call>` 的 `alignof` 产生式加同口径注释（产生式未变，不升 BNF 版本）、`IMPLEMENTATION_STATUS.md` 的 `alignof` 行由 ⚠️ 有缺陷转 ✅ 并新增「2.0 线（PB）差异」行，同项把 PA-23 遗留的「聚合体成员分隔符」状态行由 ⚠️ 改为 ✅（决策已闭环）。测试：新增 `tests/pos/alignof_builtin.nc`（9 项断言：`u8`/`i32`/`f64`/`void`/`struct`/嵌套 `struct`/`union`/`i32[4]`/`char[8]`；断言用 `puts` 而非 `print`，因 IR 前端未内建 `print`），未列入 `IR_SUBSET`，IR 后端按子集未覆盖自动跳过（IR 侧对任何 `alignof` 固定返回 8，属 2.0 待对齐）。门禁 c/native 各 **25P/0F/5S**、ir-c/ir-native 各 **6P/0F/20S**，0 FAIL。**PB 线未接入编译期对齐，实现待回灌**。**属规范承诺能力的缺陷修复，允许在冻结线执行**
- **§5.1.4 / §5.1.5 复合体指针与函数指针示例补实现（PA-26，2026-09-20，规范承诺落地）**：PA-25 只覆盖到 §5.1.2 / §5.1.3，本轮把 §5.1.4（复合类型指针）与 §5.1.5（函数指针）的剩余示例全部跑通。按"修 a+b+c，d 另登记"决策执行三项：① 切片赋值的**字符串右值** `p[a..b] = "abc"` 原先被 PA-25 的切片写分支拒绝（`slice assignment expects a value list`），现接受字面量并按**字节**整段 `memcpy`（含结尾 `\0`）；闭区间右界 `b` 即最后一个可写字节，故字面量长度须 `<= b-a`，放不下时前端即时报 `string needs N bytes with terminator, slice holds M`（复用 `ParserState.slice_len_known` / `slice_len`）。② 类型推断声明取**成员类型**：`infer_init_type` 的标识符分支此前一律返回变量自身类型，`name = xiaoming.name` 会把 `xiaoming` 的 struct 类型套到 `name` 上。现补两 token 前瞻（在 `LexerState` 副本上推进，不破坏主词法器状态）识别 `x.m`，取成员类型；结构体里的数组成员按 C 规则退化为指针（`char[9]` → `char*`）；聚合体成员保留标签符号，否则 `c_type_name` 会把变量名当类型名输出。③ 类型推断声明取**调用返回类型**：RHS 是 `f(x)` / `cb(x)` 且符号为函数类型时取返回类型（`CType.next`），§5.1.5 的函数指针赋值 `calc void(i32, i32) i32 = add` 与随后的调用推断遂可用。文档：BNF 升 **v2.7**（`<assign-stmt>` 注明切片右值两式（值列表 / 字符串字面量）与长度诊断，`<statement>` 说明补"推断声明 `<identifier> = <expr>`"的成员访问与调用两种 RHS，并注明 IR 前端属 2.0 待做）、中英 §5.1.1 隐式推断条目扩展、中英 §5.1.2 新增字符串右值切片赋值条目、中英 §5.1.4 新增"推断声明取成员类型"注。测试：新增 `tests/pos/slice_str_infer.nc`（5 项断言：切片字符串写、偏移写、`stptr.(Say).say = "..."` + `xiaoming.say` / `xiaoming.name` 推断、函数指针赋值与调用推断）与 `tests/err/slice_str_overflow.nc`（入 `IR_ERR_SKIP`，IR 前端无切片赋值路径）。门禁 c/native 各 **27P/0F/5S**、ir-c/ir-native 各 **6P/0F/22S**，0 FAIL，examples 6/7。**PB 线待回灌**（PB `parser.c` 无切片赋值分支，`infer_init_type` 标识符分支对成员访问与调用两种 RHS 均取错类型；见 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」）。同项探针另发现 `T[n] x = "literal"` 的声明容量被丢弃（生成 `char(*ch) = "..."`，且容量不足无诊断），登记为 PA-31 待决策。**属规范承诺（补回文档已承诺的写法），允许在冻结线执行**
- **`print` / `puts` 输出语义写入规范（PA-30，2026-09-20，文档-only + 回归用例）**：`print` 此前在规范中**完全没有定义**（中英 §2.3 内置函数清单里没有它），而实现早已存在且有**两种形态**，按首实参静态区分：① 首实参是字符串字面量 → 整条转发为 C `printf(...)`，第一参数即格式串，**多余参数不自动拼接、不自动换行**（故文档原示例 `print("[LOG] ", msg)` 实际只输出 `[LOG] `，`msg` 被丢弃）；② 首实参不是字面量 → 生成 `printf("%lld\n", (long long)expr)`，即按十进制整数打印该值并换行。`puts` 是 C `puts` 直通：只接受字符串指针，自动换行，喂整型会在运行期崩溃。按"文档定义 printf 直通 + 示例改格式串"决策，新增 **§2.3.1 输出内建（`print` / `puts`）**（中英同步）：三条口径 + 两形态静态区分说明 + 版本口径注（`print` 属 A 后端，`ir-*` 前端未内建，跨后端只能用 `puts`）。同项把文档中与实现相矛盾的示例全部改写：§7.4.1 的 `print("[LOG] ", msg)` → `print("[LOG] %s\n", msg)`；§14.2 的 5 处**把整型喂给 `puts`**（`puts(val.(i32))`、`puts(q.(i32)) // 输出 200`、`puts(p2.(i32)) // 100`、`puts(m.(i32)) // 900` 及 §12.1 分析表同行）改为 `print(...)` 整型形态，其中 `puts("Inspecting value: ")` 改 `print` 以避免插入换行。测试：新增 `tests/pos/print_forms.nc` + `.expect`（格式串形态打印 `char[]`、无换行 `print` 紧跟整型 `print`、`flow`+`malloc` 经 `const` 参数打印所指值），未入 `IR_SUBSET`（IR 无 `print`，自动跳过）。门禁 c/native 各 **28P/0F/5S**、ir-c/ir-native 各 **6P/0F/23S**，0 FAIL，examples 6/7。同轮把改后 §14.2 整段示例抽出编译复验，发现两项缺陷并登记待决策：`flow → flow` 所有权转移因检查顺序颠倒而在 1.0 线无法书写（PA-33）、`flow` 绑定字符串字面量会在块退出时对静态存储执行 `free`（PA-32）。**PB 线文档无 §2.3.1 口径，待回灌**；`print_forms.nc` 可作 2.0 接入 `print` 时的回归用例。**属规范承诺（补回文档缺失的语义定义），允许在冻结线执行**
- **定长字符数组的字符串初值改为真实数组存储 + 容量诊断（PA-31，2026-09-20，规范承诺落地）**：PA-26 决策"d 另登记"的遗留项。`ch char[9] = "xiaoming"` 此前直出 `char(*ch) = "xiaoming";`——声明容量 `9` 完全丢弃、退化成一个被 tcc 静默接受的指针声明器，把容量写成 `char[4]`（放不下 `"xiaoming"` 含结尾 `\0`）同样零诊断，与刚定案的切片字符串右值口径（`string needs N bytes with terminator, slice holds M`）不一致。按"补实现：数组存储 + 容量诊断"决策执行：① `parse_declaration` 在初值前判定形态——字符串字面量 + **已知容量**的数组类型时不再走"取 RHS 前缀 + 指针退化"路径，声明头按普通数组形态输出 `char ch[9]`，初值交给 C 做数组初始化（可原地改写、`len()` 按 §2.3 取声明容量）；② 新增两条前端诊断——`strlen(字面量) + 1 > 容量` 报 `string needs N bytes with terminator, array 'ch' holds M`，元素类型非 `char` 的定长数组用字面量初始化报 `cannot initialize array 'x' with a string literal; its elements are not 'char', use a value list {v0, v1, ...}`；③ 省略容量的 `char[] s = "..."`（动态字符串）与 `T[n] x = malloc(T[n])`（PA-25 的指针退化）两条既有路径均不受影响。文档：中英 §5.1.2 定案注各加"定长字符数组的字符串初值"语义条目 + 实现口径条目，BNF 升 **v2.8**（产生式不变，§2 `<array-type>` 说明定案该语义并标注 `ir-*` 两项诊断均缺）、`IMPLEMENTATION_STATUS.md` 新增状态行与三条测试覆盖行、「2.0 线（PB）差异」新增未回灌行，PA 侧 `parser.c` 行号全量重基。测试：新增 `tests/pos/str_array_init.nc`（数组存储 + 原地改写 + `len()` 取容量 + `strlen+1 == n` 边界）与 `tests/err/str_array_overflow.nc`、`tests/err/str_array_bad_elem.nc`（IR 前端两项检查均无，入 `IR_ERR_SKIP`）。门禁 c/native 各 **32P/0F/5S**、ir-c/ir-native 各 **6P/0F/27S**，0 FAIL，examples 6/7（`05_string.nc` 的 `char s[6] = "hello"` 现生成真正的数组存储）。**PB 线待回灌**；同轮复验另发现多变量声明 `var {aa = "aa",…} char[2]` 的数组后缀同样被丢弃且无诊断，登记为 PA-34（本版已按同一口径收口）。**属规范承诺（补齐 §5.1.1 / §2.3 的数组语义），允许在冻结线执行**
- **`flow` 自动释放改为只发给堆所有权（PA-32，2026-09-20，运行期安全缺陷修复）**：PA-30 探针登记的项。`flow` 变量此前在块退出与函数退出两处**无条件**生成 `free()`，没有区分右值是否为堆所有权：`flow s char[] = "字面量"` 生成 `char* s = "..."; … free(s);`（释放静态只读段）、`flow p void = &variable` 生成 `void* p = &variable; … free(p);`（释放栈地址），两者都静默编译、运行期中止且无任何输出。按"登记存储来源，free 只发给堆右值"决策执行：① `Symbol` 新增 `no_auto_free`（零初始化默认 0，即默认行为与既有释放一致，只有明确登记为非堆来源者被豁免）；② `parse_declaration` 按右值首 token 登记来源——字符串字面量（静态只读段）、`&x`（他方存储）、`{...}`（聚合存储）三类豁免，`malloc` 系等仍按堆所有权释放；③ `parse_assign` 的 `=` 分支对**裸标识符 LHS** 的 `flow` 变量按新右值重登记（`p.(T) = v`、`p[i] = v` 改的是所指对象，不动标记）；④ 两处退出点各加 `&& !s->no_auto_free`。刻意保留的代价：堆所有权被重绑定后旧堆块泄漏（不在重绑定点插入释放，避免经别名双释放），精确回收属 2.0。文档：中英 §11.1 新增"四类右值来源表 + 重绑定与泄漏口径"，中英 §14.1 的 `flow person_ptr void = &some_person` 补"该形态不自动释放"注，`IMPLEMENTATION_STATUS.md` 的 ⚠️ 运行期缺陷行转 ✅、新增已知限制行与「2.0 线（PB）差异」行，PA 侧 `parser.c` 行号重基。测试：新增 `tests/pos/flow_no_free.nc`（字面量／取址／堆／堆→栈重绑定四形态），未入 `IR_SUBSET`（IR 前端无所有权与释放路径，自动跳过）。门禁 c/native 各 **33P/0F/5S**、ir-c/ir-native 各 **6P/0F/28S**，0 FAIL，examples 6/7。**PB 线待回灌**（PB 两前端仍无条件 `free`）。**属运行期安全缺陷修复（消除对静态／栈存储的非法释放），允许在冻结线执行**
- **`flow → flow` 所有权转移补通（PA-33，2026-09-20，规范承诺落地 + 相邻检查补齐）**：§12.1 传递矩阵与中英 §14.2 一直承诺「`flow` 源 → `flow` 目的 = 所有权转移，源失效」，但该形态在 1.0 线**完全写不出来**——`vis_check_assign` 在检查阶段就把源标记 `BS_INVALID`，随后生成右值时才经 `vis_check_usable` 读取该源，于是同一条语句必然自报 `'ptr' is invalidated: its ownership has been moved`；声明初始化（`flow new void = ptr`）与普通赋值（`new = ptr`）两条路径同时命中。按"局部豁免本语句自身源"决策（未选调整检查顺序）实现：① `ParserState.moved_src` 由 `vis_check_assign` 在置失效时登记，`vis_check_usable` 只对**当前语句自身**的转移源放行，下一条语句进入 `parse_statement` 即清除——「失效」自下一条语句起生效，与文档一致；② 转移同时置源的 `ownership_transferred`，使失效源退出时不再 `free`，释放责任移交接收方（否则同一堆对象被释放两次，实测转移链只生成一处 `free`）；③ 同轮按 §14.2 分析表复验发现并补齐相邻缺口：源正被 `const`/`var` 借用（冻结）时转交所有权此前**静默通过**，现即时报 `cannot transfer ownership of 'ptr': it is borrowed (frozen) by an active const/var`。文档：中英 §12.1 状态说明新增三条口径（失效的生效边界 / 冻结源不可转交所有权 / 转移即移交释放责任），中英 §11.1 来源表新增"已转移出去的源"一行并说明转移不属泄漏，中英 §14.2 的 `flow new_owner void = ptr` 行内注释由「ptr,read_ref,mut_ref 失效」改为「错误：源正被借用」（消除与该表下方分析行"检查失败"的自相矛盾），BNF 升 **v2.9**（产生式不变，新增实现口径注 + IR 前端不做所有权/借用检查）、`IMPLEMENTATION_STATUS.md` 该行 ⚠️→✅ + 两条测试覆盖行 + PB 差异行，PA 侧 `parser.c` 行号重基（+3）。测试：新增 `tests/pos/flow_move.nc`（声明式 + 赋值式转移、`&x` 来源经转移后重登记为堆所有权）与 `tests/err/flow_move_frozen.nc`（入 `IR_ERR_SKIP`）。门禁 c/native 各 **35P/0F/5S**、ir-c/ir-native 各 **6P/0F/30S**，0 FAIL，examples 6/7。原 `tests/err/m2d_invalid.nc` 此前"通过"属**假阳性**（错误报在转移语句本身而非其后的使用行），现报在真正的 use-after-move 行。**PB 线待回灌**（PB `vis.c` 无 `moved_src` 与冻结源守卫，两写法同样不可写）。**属规范承诺（补回文档承诺的所有权转移形态），允许在冻结线执行**
- **多变量声明的定长数组改为真实数组存储（PA-34，2026-09-20，规范承诺落地 + 静默错误消除）**：PA-31 同轮登记的遗留项。`var {aa = "aa",bb = "bb",cc = "cc"} char[2]`（中英 §4.2 示例形态）此前在 `parse_declaration` 的多变量分支只输出 `c_type_name(&bt)` + 变量名，声明的数组后缀 `[2]` **整体丢弃**，字面量赋给标量 `char`——tcc 只报 `assignment makes integer from pointer without a cast` 警告，ncc 前端零诊断，生成的 C 语义全错；且声明期未登记 `Symbol.len_known`（PA-27/PA-31 的登记只覆盖单变量路径），同体 `len(aa)` 报"逻辑长度静态不可知"。按"多变量分支复用 PA-31 判定"决策执行：① `arr_cap = type_array_count(&bt)` 判定已知容量后，声明器输出补 `c_type_suffix(&bt)`，每个变量各自得到按声明容量分配的**数组存储**（`char aa[3]`）；② 逐变量复用 PA-31 的两条诊断（超容量 `string needs N bytes with terminator, array 'aa' holds M`、元素非 `char` 须改值列表），文案与单变量完全一致；③ 登记 `len_known`/`logical_len`，并顺带为省略容量的 `{…} char[]`（探针确认该形态此前就正确生成 `char* aaa = "aaa"`）登记字面量长度，使 `len()` 在多变量路径与单变量路径口径统一。新增第三条诊断（决策外的同分支缺陷）：定长数组类型的多变量声明遇**非字符串字面量**初值（如 `{x = 1,y = 2} i32[4]`）报 `has fixed array type but no string literal initializer; use a single-variable declaration for this form`——该形态此前同样静默生成 `int x = 1;`，而单变量分支的对应处理依赖整段 RHS 重排，不在多变量分支复制。**副带效应**：落地后中英 §4.2 原示例 `char[2]` 自身即超容量（`"aa"` + NUL = 3 > 2），示例遂改为 `char[3]` 并加口径注释——任何方案都要改该行。文档：BNF 升 **v2.10**（§4 说明新增"多变量声明的定长数组口径"条，产生式不变）、中英 §5.1.2 定案注各加"多变量声明的同形态"条目并把实现口径改为 PA-31/PA-34、`IMPLEMENTATION_STATUS.md` 该行 ⚠️ 缺陷→✅ + 三条测试覆盖行 + 「2.0 线（PB）差异」未回灌行，PA 侧 `parser.c` 行号全量重基（多变量分支净增 36 行）。测试：新增 `tests/pos/multi_arr_decl.nc`（定长数组存储 + `len()` 取容量 + 动态 `char[]` 取字面量长 + 标量形态不受影响）与 `tests/err/multi_arr_overflow.nc`、`tests/err/multi_arr_nostr.nc`（三条均不入 `IR_SUBSET` / 入 `IR_ERR_SKIP`，IR 前端不支持该声明形态）。门禁 c/native 各 **38P/0F/5S**、ir-c/ir-native 各 **6P/0F/33S**，0 FAIL，examples 6/7。**PB 线待回灌**（PB `parser.c` 多变量分支与修复前逐字相同，可直接移植，三条用例作回归）。**属规范合规（补齐 §4.2 示例承诺的形态并与 §5.1.2 定案对齐），允许在冻结线执行**
- **编译器版本号改为构建期单一真源（PA-35，2026-09-20，一致性核对 C1）**：`NIHAO_VERSION` 原写死 `"0.1.0"`，用户可见的三处输出（`--version`、帮助横幅、`-v` verbose 启动行）全部打印 `v0.1.0`，与 `xmake.lua` 的 `set_version("1.0.2")` 及本日志/ROADMAP 的 v1.0.2 发布口径直接冲突。按"构建期注入做单一真源"决策：① `xmake.lua` 顶部新增 `nihao_version` 常量，`set_version(nihao_version)` 与 `add_defines("NIHAO_VERSION_TAG=" .. nihao_version)` 同源于此**一处**（`get_config("version")` 在本工程解析期返回 nil，故不采用回读配置）；② `ncc.h` 撤掉独立版本常量，经两级字符串化宏把注入的裸 token 转成字符串，未注入时（LEGACY `Makefile` 直编，入口已标注弃用）落到占位值 `"0.0.0-unknown"` 并注明其不代表发布口径；③ `nihao init` 生成的 `nihao.toml` `[project] version` 原为另一处硬编码 `0.1.0`，随决策改为拼接 `NIHAO_VERSION`（脚手架项目版本即编译器版本）。实测 `ncc --version` 与帮助横幅输出 `NihaoC Compiler v1.0.2`、`init` 产出 `version = "1.0.2"`。文档：`VERSIONING_ROADMAP.md` §3.2 新增"版本号单一真源"条款（发布时只改一处 + 占位值口径），`GIT_CONVENTIONS.md` §1 版本化与发布加"发布前必查版本号"门禁项。测试无版本断言，门禁复验不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S，0 FAIL）。**PB 线待回灌**（PB 的 `ncc.h`/`xmake.lua` 仍是两套值，移植时把 `nihao_version` 置为 PB 的发布版本）。**属代码卫生（消除同义两值），允许在冻结线执行**
- **README 的现在时计数换为 v1.0.2 现值（PA-36，2026-09-20，一致性核对 C2，文档-only）**：README「1.0 范围」条仍写「全量语法回归 24P/0F，examples 6/6 通过」，与 v1.0.2 实测（每后端 **38P/0F/5S**、examples **6/7**）不符。按"换为现值 + 加权威指针"决策：① 该条改述为 v1.0.2 门禁实测数字；② 补一句 `06_cooking.nc` 的失败归因与后端差异（`cooking` 编译期块与编译期函数尚未纳入 1.0 语法，实测 1.0 线报 `cooking const: expected '='`、`ir-c` 编译通过），避免读者把 6/7 当缺陷；③ 加"计数随版本变化，权威口径见 `CHANGELOG.md` 当前版本段与 `docs/IMPLEMENTATION_STATUS.md`"的指针，防止再次漂移；④ 相邻「双平台验证（2026-08-31）」条的 `examples 6/6` 属当时事实，保留原文并补「v1.0.0 时点」「当时的用例集」定语标为历史快照。同类过时计数（`STAGE_SUMMARY.md` 的 12P/26P/56 PASS、`ROADMAP.md` 的 PB 验收行 73 PASS/26 SKIP）按 C4/C5 各自决策，不在本项并入。无代码与测试改动，门禁基线不变。
- **ROADMAP 架构图重写为真实目录树（PA-37，2026-09-20，一致性核对 C3，文档-only）**：§二「当前代码架构」原图含三个**不存在**的路径（`ncc/test/`、`ncc/arch/`——原文还写着"目录已建，内容为空"、`ncc/myapp/`），并把 `前端` / `后端` 画成子目录（`ncc/` 实际全部 `.c/.h` 扁平放置），手工示例仍挂在 `ncc/test/` 而真实位置是仓库根 `examples/`；另缺 `ir_backend.h`、`build/`，且 `Makefile` 与 `xmake.lua` 并列称"双构建系统"（该文件顶部已标 LEGACY 弃用）。按"重写为真实目录树"决策：仓库根 → `docs/` → `examples/` → `ncc/` 按实际层级绘制，职责改用 `[前端] / [前端·A 线] / [前端·IR 线] / [后端]` 行内注释区分，补 `ir_backend.h`、`build/`（注明由 `ncc/.gitignore` 忽略）与 `editors/sublime/` 实际内容，`Makefile` 标 LEGACY、`xmake.lua` 标"版本号单一真源"。PA 与 PB 的 `ncc/` 根文件清单经 `git ls-tree` diff 确认逐字相同，故本图对两条线通用；幽灵项在其余文档无引用（全仓 grep 确认）。文档-only，无代码与测试改动，门禁基线不变。
- **STAGE_SUMMARY 定性为历史快照并加权威指针（PA-38，2026-09-20，一致性核对 C4，文档-only）**：`docs/STAGE_SUMMARY.md` 标题写着「2026-08-30 / 更新至 2026-09-01」的阶段快照，正文却以现在时陈述现状：§2「全量 parser 测试 12P+4S」、§3「IR 用例 31 个」、§4「c/native 12P、IR 一致性 26P」「**当前** 56 PASS」、§3.1「当前语法以 `BNF.md` **v2.2** 为准」（实际已 v2.10），且第 3 行仍把「读本文件」列为会话恢复入口——README/VERSIONING_ROADMAP/ROADMAP/PB24_DECISION 四处引用它，读者极易把 8 月口径当成 v1.0.2 现值。按"标为历史快照 + 权威指针"决策（未选刷新为现值，避免与 CHANGELOG/ROADMAP 形成第三份现状文档；未选拆成归档，因 §3.1 矩阵是 `PB24_DECISION.md` 引用的决策原始依据）：① 文件头加横幅——本文件为快照、口径定格于 2026-09-01，现状改指 `CHANGELOG.md` 当前版本段 + `IMPLEMENTATION_STATUS.md` + `BNF.md` 最新版 + `ROADMAP.md`，恢复入口改为 `README.md` + `CHANGELOG.md` + `TODO.md`，并列明本文件两项不可复得价值（§3.1 决策依据、§3「关键坑」实测记录）；② §2/§3/§4/§5 标题加「快照至 2026-09-01」定语，四处现在时计数各补一句 v1.0.2 现值（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S），§4 命令行注释去计数并单列「快照计数 vs 现值」对比；③ §3.1 快照注补第三轮收敛（`?.`/`?(` 移除、检查落入 `.()`／PA-21），「以 BNF v2.2 为准」改为「以最新版为准（v1.0.2 时为 v2.10）」；④ §1 两条随双仓库合并过时的表述（内部活跃仓库 `D:\workspace\workbudy\ncc` 与发布副本 tar 同步、`credential.helper=wincred` 可直推）标注**已废止流程**并改写为现行做法（单仓库 `PA`/`PB` 分支 + 共享文档 `git merge-file` 三方合并；推送与 tag 由所有者逐次授权），消除与「禁止自动推送」规则并读时的误导；⑤ §5 加盘点注，就地澄清三条已完成/未去重项（PA-9 已实测完成、link `-l` 传递已实现、中英规范已核对至 BNF v2.10）。连带 README 顶部导航链接标签由「进度总结」改为「阶段总结（历史快照）」。**共享文档范围确认**：本文件与 `PB24_DECISION.md` 在 PA/PB 两分支的已提交内容逐字相同（`git diff PA PB --` 仅 README 有差），故纳入跨分支同步集，README 第 26 行两分支亦一致。**PB 线待回灌**（本文件 + README 导航标签）。文档-only，无代码与测试改动，门禁基线不变。
- **里程碑表计数写法约定 + PB 验收行去无法溯源的数字（PA-39，2026-09-20，一致性核对 C5，文档-only）**：`docs/ROADMAP.md` 里程碑表「验收 | PB `xmake test --all` 全矩阵 73 PASS / 0 FAIL / 26 SKIP」一行既无日期，`73`/`26` 又在 PB 分支 `TODO-PB.md` 中找不到对应记录（PB 侧只记「全矩阵 0 FAIL」与分阶段数字），而本行位于跨分支共享文档、PA 分支既无法实测也无从更新——与 README（PA-36）、STAGE_SUMMARY（PA-38）同属"把单分支某时点的详细计数写进共享文档"的漂移模式。按"只保留 0 FAIL + 日期 + 指向 TODO-PB"决策：① 该行换为不变量口径「**0 FAIL**（PB-25 ~ PB-29 阶段验收，2026-09-04 ~ 09-16；PASS/SKIP 详细计数随 PB 线用例增删波动，权威口径见 PB 分支 `docs/TODO-PB.md`）」；② 里程碑表上方新增「本表计数写法约定」三条（只写不随用例增删漂移的不变量且必带日期／详细 PASS/FAIL/SKIP 计数写分支专属 TODO／仅发布行可写门禁计数且必须绑定版本号，权威详值指本日志对应版本段），并注明约定自 2026-08-29 分支化之后的行起适用，骨架期各行原样保留；③ 据此把本文件 v1.0.2 发布行的门禁计数绑定版本号（「v1.0.2 门禁 …，权威详值见 `CHANGELOG.md` v1.0.2 段」），文件头更新日期上抬；④ 规则升入 `GIT_CONVENTIONS.md` §1 新增「共享文档的测试计数写法」一条（共享层只写不变量＋版本/日期，详细计数入分支专属 TODO 与 CHANGELOG；快照文档须标快照日期＋权威指针、不得以现在时陈述现状）。文档-only，无代码与测试改动，门禁基线不变。**PB 线待回灌**（`ROADMAP.md`、`GIT_CONVENTIONS.md` 均为共享文档）。
- **GIT_CONVENTIONS 三处失实/失效陈述就地修正（PA-40，2026-09-20，一致性核对 C6，文档-only）**：① §一「分支规则」以现在时写「三条分支均从同一稳定点分出（当前基于 `main` 的 `04f1435`）」——实测 `main` 已前进到 `90c1742`（2026-09-19）、`PA` 为 `dc2dac0`（2026-09-20），且 `git merge-base PA PB` 为 `7d4c652`（2026-08-31）而非分支化点，改为带日期的历史陈述并补实际共同祖先；② §一「版本化与发布」末尾仍规定「双仓库（NihaoC ↔ ncc 活跃）必须成对提交防基线漂移」，与 PA-38 定案的「双仓库同步流程已废止」直接冲突（本文件又是约束后续工作的规则文档），改为废止声明 + 现行做法（单仓库 `PA`/`PB` 分支，共享文档 `git merge-file` 三方合并），语法演进/bug 修复的双向顺序不动；③ §五 两处「（见 §1 发布门禁）」为失效交叉引用（§1 无该小节，推送硬门禁在 §3.1「硬性约束：禁止自动推送」），改指 §3.1；同类引用在 `TODO-PA.md` 的 PA-35 记录一并订正。**未选**：在「分支规则」补 commit 锚点防漂移约定（仅修正事实）。同轮登记相邻待决策项 **C6b**：`docs/VERSIONING_ROADMAP.md` §3.3/§3.4/§3.6 仍以现在时规定已废止的双仓库流程，§3.5 门禁表 PB 行挂「56 PASS」单分支计数。文档-only，无代码与测试改动，门禁基线不变。**PB 线待回灌**（共享文档）。
- **PA TODO 处理完毕**：PA-1 ~ PA-40 全部 `[x]`；A/B 组（实现与文档承诺）已收口，C1（版本号口径）、C2（README 计数）、C3（ROADMAP 架构图）、C4（阶段快照定性）、C5（里程碑表计数约定）与 C6（GIT_CONVENTIONS 失实/失效陈述）已定案落地，剩余为文档-实现一致性核对 C 组（C6b 双仓库章节、表格残缺等零星项）与 D 组（语法元素表 `=>` 行残留、v1.0.2 tag 与推送）待决策项。
- **已知缺口（登记于 `docs/IMPLEMENTATION_STATUS.md`）**：1.0 线 A 后端 `is <identifier>` 变量绑定为"按值比较"而非绑定（2.0 线覆盖）；函数参数可见性前缀仍统一按 `VIS_DEFAULT` 处理；IR 后端不做所有权/借用检查。
- **共享文档 PA↔PB 双向同步（2026-09-19）**：以 `merge-base 7d4c652` 三方合并把本版本成果同步进 PB（PB 侧 `37a112a`），并把 PB 侧更新的 4 项回灌本线——① 中英 §5.1 指针声明节定案表述（"隐式推断声明"，原"双支持"自 v1.0.1 起过时）；② BNF `<array-size>` 细目 + 动态数组写法三条说明 + `<is-clause>`/`<pattern>` 注释；③ `VERSIONING_ROADMAP.md` 的 2.0 进展条目（阶段1 类型化指针、PB-26 M2 移植、ir-c 变量名保留已由 `[ ]` 转 `[x]`）与 A 方案独占文件清单残留的 `codegen.c` 修正；④ `IMPLEMENTATION_STATUS.md` 增补「2.0 线（PB）差异」节并修正 `pattern.nc` 覆盖描述。`docs/LEGACY_CODEGEN.md` 归档文档一并纳入共享集。同步后两条线共享文档一致（`TODO-PA.md` / `TODO-PB.md` 为分支专属，不计入）。
- **回归验证**：c / native 双后端各 **38P / 0F / 5S**、ir-c / ir-native 各 **6P / 0F / 33S**（0 FAIL，含 PA-18 ~ PA-22、PA-25 ~ PA-27、PA-31、PA-34 十四条新 err 用例与 `ownerof` / `deref_slice` / `len_builtin` / `elseif_chain` / `alignof_builtin` / `slice_str_infer` / `print_forms` / `is_no_fallthrough` / `str_array_init` / `flow_no_free` / `flow_move` / `multi_arr_decl` 用例），跨后端一致性检查全部通过；examples 6/7 编译运行（`06_cooking` 为 2.0 预览，预期不通过，IR 后端下可通过）；`pattern.nc` 经 ir-c / ir-native 实测输出与全量后端一致。

## [v1.0.1] — 2026-09-03

1.0.x 首个补丁版本：指针语法收敛 + Linux 平台修复 + 规范文档定案。

- **移除带 `*` 的指针语法形式（与 BNF/文档一致）**：删除具名指针类型声明 `T*`（如 `p T* = &x`）与一元 `*` 解引用（`*p` 读/写/复合 `*p op=`）；指针解引用统一收敛为 `.()` / `.(T)` / `->`。`void` 通用指针、`void[n]` 指针数组、隐式推断 `p = &x` 与乘法 `*` / `*=` 均保留（属不同语义，非指针语法）。
- **编译器（A 方案 parser.c）**：`parse_type` 删除 `TOK_STAR` 指针构造分支（仅保留 `[]` 数组后缀，并修正 `while` 条件避免 `*` 死循环）；`parse_unary` 删除一元 `*` 解引用分支（落入默认报错）。内部 `TYPE_POINTER` 类型与 `cgen.c` 的 `T*`/`void*` 文本映射**保留**（生成可编译 C 的前提，与用户输入语法无关）。
- **示例与测试同步改写**：`examples/04_pointer.nc`、`tests/pos/ir_ptr.nc`、`ir_ptr2.nc`、`ir_slice.nc` 的 `*p` / `*(&x)` / `*p op=` 全部改为 `.()` 形式；复合 `*p += e` 展开为 `p.() = p.() + e`（双后端兼容写法）。
- **回归验证**：c/native 双后端 **13P / 0F / 5S**（0 FAIL），`examples/04_pointer.nc` 运行输出 `deref write ok` / `addr deref ok`。
- **B 方案（PB）待办登记**：`irparse.c` 仍支持一元 `*p`（读/写/复合 RMW）且 `.() op=` 缺失，已登记 `TODO.md` 专项二 PB-25（移除一元 `*p` + 补 `.() op=`，本次 PB 不实现）。
- **`is` 模式匹配规范定案（仅文档）**：`is` 仅配合 `while`（`do` 不支持）、移除 `=>` 箭头形式；BNF 重写 pattern 产生式；中英 §6.1 新增模式匹配子节（语义表 + R1–R4）。语法实现变更落在 v1.0.2（PA-13）。
- **§12 指针传递矩阵文档统一**：合并 §12.1+§12.2 为单一 §12.1（4×4 矩阵为主、16 条枚举为辅）；§12.3→§12.2 重编号；新建 `docs/IMPLEMENTATION_STATUS.md` 规范-实现对应表。

### Linux 平台修复（2026-08-31 WSL 实测，PA-9）

- `xmake.lua`：os.exec 在 Linux 不走 shell（`|| true` 被当作程序参数）→ Linux 分支显式 `/bin/sh -c` 包装；非 Windows 跳过 p0_link target 与 ir-native 测试
- `cgen.c`：c_type_name 三处 static buf 重叠写（递归调用 src==dst，Linux glibc 损坏输出）→ 独立 `char tmp[256]` 拷贝
- `native.c`：源码构建 libtcc.so 未导出 `tcc_install_dir`（隐式声明 int 截断指针）→ 非 Windows 硬编码 `/usr/local/lib/tcc`
- `native.c`：`-run` 内存执行误判 `tcc_relocate(s, NULL)` 返回值（NULL 语义为"返回所需内存大小"，>0 即成功）→ 改为直接 `tcc_run`（内部自动 relocate）
- 构建依赖：libtcc.so 内部符号（sym_push 等）与 ncc 重名被 ELF 符号插值劫持 → 以 `make libtcc.so LDFLAGS="-fPIC -Wl,-Bsymbolic"` 重新构建安装

## [v1.0.0] — 2026-08-31

**首个对外版本（A 方案产品线）**。A 方案（parser → C 文本 → tcc）功能闭环，特性冻结，只修 bug/文档。

### 功能（A 方案 c/native 后端）

- 全量语法回归 **12P / 0F / 5S**（c/native 双后端一致）
- 指针声明语法定案：**隐式推断声明**（`p = &x` 自动推断为指向 x 的指针）；`->` 指针成员访问（链式/复合赋值）；1.0.x 起具名指针 `T*` 显式声明与一元 `*` 解引用已移除，解引用统一 `.()` / `.(T)` / `->`
- 三元 `?:`、`is pat => stmt` 单语句匹配（`=>` 新 token；已于 1.0.2 / PA-13 与 2.0 / PB-27.6 移除，`is` 只保留块形式）、goto/label
- struct/union/enum（嵌套、位域、嵌套初始化列表、整体拷贝）、数组/动态数组（固定容量）、切片、多返回值（命名 struct 返回，C 机制）
- 存储期与所有权（const/static/flow/var + 借用状态机）、cooking 编译期（常量/函数/static_assert）、len()、visof()
- 后端 `-backend=native`：libtcc 进程内编译执行；`-run` 内存执行（Linux only）

### 工程质量

- CLI 完整：init/build/run/debug/lex + -o/-c/-shared/-static/-I/-L/-l/--link/-g
- 构建统一 xmake（Makefile 标 legacy）；错误信息"外壳中文 + 正文英文"
- examples/ 示例集 7 例（6 例 1.0 可编译运行，06_cooking 为 2.0 预览）
- 语言规格冻结：BNF v2.0 终校（`=>`/`->` 补全）+ 中英语法元素表核对；1.0.x 起 `T*` 具名指针与一元 `*` 解引用已从 BNF 移除
- **双平台验证（Windows + Linux/WSL Ubuntu-24.04）**：c/native 全量回归 0 FAIL（12P/0F）+ examples 6/6 双后端一致；`-run` 内存执行 Linux 实测通过

### 明确留给 2.0

- 动态数组增长、切片运行时边界、命名空间
- IR 一切（ir-c/ir-native/ir-riscv64/ir-arm64/ir-loongarch64，当前为 2.0 预览）
- native 寄存器分配（性能项）；ir-native 在 Linux ELF 运行时崩溃（2.0 预览，Windows PE 正常）

## [2.0-dev] — 未发布（B 方案 IR 演进线）

- **阶段 1 类型化指针模型（2026-08-31）**：`p = &标量` 记录指向类型（pt[] 表 PT_SCALAR 编码，聚合/标量/枚举三分支）；解引用读按指向类型标记浮点；解引用写按指向类型 coerce（窄型 TRUNC 截断 / double ITOD）；复合赋值 RMW；指针算术 `p + k` ×8 槽宽缩放（与数组寻址一致）。新用例 ir_ptr2.nc（IR_SUBSET）读/写/narrow 截断/double/复合+算术 5 组断言，四后端一致 0 FAIL

### 2.0 指针语法收敛（2026-09-04，对齐 A 方案 1.0.x）

- **移除一元 `*p` 解引用（与 BNF/文档一致）**：`irparse.c` 删除一元 `*` 解引用读（`ir_primary`）、`*p = e` / `*p op=` 写+复合（`ir_stmt` 两处），改为 `nihao_error` + 吞 token 防死循环；乘法 `*` 不受影响。指针解引用统一收敛为 `.()` / `.(T)` / `->`。`void` 通用指针、`void[n]` 指针数组与隐式推断 `p = &x` 保留。
- **补 `.() op= e` 复合解引用**：`ir_stmt` 的 `.() = e` 写块支持 `+= -= *= /= %=`——LOAD 当前值 → 按指向类型协调（double 指向走 FADD/FSUB/FMUL/FDIV 且 LOAD 结果直接 `ir_set_double` 勿 ITOD）→ op → 按指向类型 `ir_coerce` 截断 → STORE。
- **修复 2 个既有 IR 后端正确性缺陷**（此前被 `ir_ptr2.nc` 无 `.expect` + 跨后端一致性检查"错得一样也算 PASS"掩盖）：① `.() read` 对浮点指向（f64/f32）标记结果 vreg 为 double，避免位模式被当整数 ITOD 误转；② `.() = e` / `.() op=` 按**指向类型** `pt[vi]` 截断（窄型 TRUNC / double→int 兜底），原代码误用指针自身类型 `vtype[vi]` 导致窄写不截断。
- **测试同步改写**：`ir_ptr2.nc` 复合解引用改为 `p.() += 2` / `pd.() += 1.0` / `pc.() += 1` / `p.() *= 3` / `p.() -= 10`（含 int/double/narrow/mul/sub），四后端一致 0 FAIL；`ir_ptr.nc`/`ir_slice.nc` 保持通过。
- **回归验证**：`xmake test --all` 全矩阵（c/native/ir-c/ir-native）**0 FAIL**，`ir_ptr`/`ir_ptr2`/`ir_slice` 四后端一致。

### 2.0 M2 能力平移：参数前缀 + 返回值可见性（2026-09-04 / 09-10 / 09-12，PB-26 / PB-29）

- **PB-26 参数前缀（2026-09-04）**：函数参数声明支持 `flow/var/const/static` 可见性前缀，记录到变量表 `vvis`；调用点按参数前缀执行 M2 所有权/借用检查（冻结/失效状态机），`err/m2a..m2e` 系列用例在 ir-c/ir-native 双后端转正 PASS。
- **PB-29 返回值可见性前缀（2026-09-10，§12.3 后半段）**：函数声明 `func name(...) [vis] ret_type` 解析返回前缀到 `IrFn.ret_vis`（IR 端）/`Symbol.ret_vis`（cgen 端）；调用点声明赋值按 `ir_vis_transfer(ret_vis, decl_vis)` 检查所有权转移；cgen 端 `flow/const/static` 返回生成 `void*` 签名。新增用例 `pos/return_flow.nc`、`pos/static_param_share.nc`、`pos/struct_param_prefix.nc`、`err/m2f_retflow.nc`。
- **PB-29.1 错误路径补全（2026-09-12）**：`m2f_retflow` 六条禁止路径全覆盖（`flow→static`、`const→flow/static/var`、`static→flow/var` 等），拆分为 6 个独立 err 用例。

### 2.0 `is` 模式匹配升级（2026-09-16，PB-27，规范定案 2026-09-01）

- **Bug 修复**：`do` 循环内 `is` 拒绝（不再设置 `__is_val`，防静默匹配外层 while）；畸形模式 `is -` / `is 5..` 强制校验后续 token；反向范围 `is 5..2` 编译期报错（lo≤hi）；irparse 冗余 `VIS_*` 宏删除，与 `ncc.h` Visibility 枚举统一（`VIS_UNDEF=0/VIS_CONST=1/VIS_FLOW=2/VIS_STATIC=3/VIS_VAR=4`，`VIS_DEFAULT` 全代码更名 `VIS_VAR`）。
- **模式扩展**：通配符 `_` 恒匹配（IR 与 A 端 parse_is_stmt 同步支持）；`__is_val` 类型感知（新增 `emit_is_cmp_with_const`，double 条件走 `IR_FCMP`，不再硬编码 int）；清理重复不可达可见性分支；**移除 `is <pat> => <stmt>` 单语句形式**（A 端 PA-13 已于 09-15 移除，`=>` 保留词法识别但语法不使用，见 BNF v2.2）。
- **附带修正**：PB-29 M2 返回检查条件 `ret_vis > VIS_VAR`（VIS_VAR=4 恒假）改为显式 `== CONST/FLOW/STATIC`。
- **回归**：`xmake test` 全矩阵 0 FAIL（c/native 19P/0F/6S；ir-c/ir-native 13P/0F/8S）；`err/m2f_retflow*` 6 条禁止路径全 PASS。

## [M4] — CLI 工具链

- `nihao init/build/run/debug/lex` 子命令；`debug <file> --ir` 三地址码 dump
- 回归测试套件（tests/pos + tests/err，xmake 驱动全矩阵）

## [M3] — 模块与自动释放

- `use` 跨文件解析、`flow` 自动释放、`print` 映射、`main` 返回 0

## [M2] — 静态分析

- 所有权/借用静态分析：存储期矩阵 + 冻结/失效状态机 + 作用域解冻

## [M1] — 核心编译管线

- 表达式/声明/函数/语句解析 + C 后端单趟转 C（端到端可运行）
- 修复数组/位域/for/别名/while 系列 bug

## [M0] — 骨架复活

- 统一 token 命名、补关键字、词法最长匹配、恢复编译管线

## 基线

- 导入 ncc 骨架现状
