# 方案 A — libtcc native 后端待办（PA 分支）

> 更新日期：2026-09-21（PA-1 ~ PA-47 全部完成（PA-35 = 一致性核对 C1 版本号、PA-36 = C2 README 计数、PA-37 = C3 架构图、PA-38 = C4 阶段快照口径、PA-39 = C5 里程碑表计数约定、PA-40 = C6 GIT_CONVENTIONS 失实/失效陈述、PA-41 = C6b VERSIONING_ROADMAP 双仓库废止注、PA-42 = C7 中英规范标题编号与层级、PA-43 = C8 语法元素表「其他运算符」补行＋英文表残缺 bullet、PA-44 = C9 `vis.c` 注释章节号、PA-45 = D1 语法元素表 `=>` / `?=` 行判为分支差异并结案、PA-46 = D2 `main` 镜像陈述绑日期 + PA→main 本地合入、PA-47 = D3 `v1.0.2` 本地附注 tag）；**一致性核对 A/B/C/D 四组全部收口，无待决策项**；`v1.0.2` 已于 2026-09-21 打本地附注 tag → `18f9c77`，**唯一剩余动作是把 `PA`/`main`/`PB` 与 tag 推送到远端，由 ltree 逐次授权（`GIT_CONVENTIONS.md` §3.1）**）

> 2026-09-21 **语法设计固定轮（PA-48）** 完成：R1~R19 裁定落地进 `BNF.md` v2.11 与中英规范、两份语法元素表，实现状态整体剥离到 `IMPLEMENTATION_STATUS.md`；同轮实测（F1~F9）暴露的代码/设计差异登记为 **PA-49 ~ PA-53 五条开放待办**（成员默认值 lowering、字面量转义、四档数组容量写法、`linkas` 无实现、死 token 冻结决策）。文档-only，不动代码，门禁基线不变。
> 本文件为 PA 分支（1.0 冻结线）专属待办。通用里程碑与跨分支待办见 `ROADMAP.md`。

> PA 分支处于冻结维护态，仅接受规范合规修复与代码卫生项。

---

## 已完成

- libtcc 封装（tcc_new → compile_string → output_file / run）；目录探测（NIHAO_TCC_DIR > PATH）
- `-backend=native` 可执行文件模式实测通过；`-run` Linux 路径已实现、Windows 明确报错
- xmake 直链 libtcc.dll / -ltcc；`xmake test -b native` 支持

---

## 待办

- [x] **PA-1 `-run` Windows 不可用（已于 2026-08-06 文档化闭环）**：libtcc 0.9.27 Windows 版 TCC_OUTPUT_MEMORY 损坏（relocate 251）。决策：**正式文档化 Linux-only** —— README 新增「-run 内存执行（Linux only）」小节（含原因/替代方案/能力开关/参数透传说明）；帮助信息已标注 `(Linux only)`；Windows 下报错明确（native_memory_available() 检查）。修复/升级 libtcc 留作独立攻坚项（PA-1b，暂不进行）
- [x] **PA-2 native 无自动化回归（已于 2026-08-05 完成）**：`xmake test -b native` / `--all` 全矩阵覆盖，native 13/13 通过（测试体系已统一为 xmake，python run_tests.py 已移除）
- [x] **PA-3 `-g` 接入（已于 2026-08-06 完成）**：native_state 按 debug_mode 加 `-g`，native_compile_string/native_run_string 接收 debug 参数
- [x] **PA-4 Makefile legacy 化（已于 2026-08-06 完成）**：Makefile 顶部标注 LEGACY（构建统一走 xmake，注明缺 libtcc.h 等已知缺陷），避免误用
- [x] **PA-5 双测试脚本不一致（已于 2026-08-05 解决）**：测试统一为 `xmake test [-b ...] [--all] [-f ...]`，run_tests.py 删除
- [x] **PA-6 `-run` argv 透传（已于 2026-08-06 完成）**：CompilerState 加 run_argc/run_argv，`-run` 之后参数透传 main；Windows 不可用（PA-1）
- [x] **PA-7 link 库声明（已于 2026-08-06 完成）**：native_state 遍历 cs->link_libs 调 tcc_add_library
- [x] **PA-8 错误信息包装（已于 2026-08-06 完成）**：tcc_set_error_func 回调统一输出 `native: <msg>`
- [x] **PA-9 Linux 路径实测（2026-08-31 WSL Ubuntu-24.04 完成）**：c/native 各 12P/0F/6S（0 FAIL）；examples 6/6 双后端；`-run` 内存执行修复（tcc_relocate(NULL) 语义误判 → 直接 tcc_run）；实测修复 4 个平台 bug：① xmake os.exec Linux 不走 shell（`|| true` 被当参数）→ `/bin/sh -c` 包装；② c_type_name static buf 重叠写（递归调用 src==dst，Linux glibc 损坏）→ 独立 tmp 拷贝；③ 源码构建 libtcc.so 未导出 tcc_install_dir → 非 Windows 硬编码 /usr/local/lib/tcc；④ libtcc.so 内部符号（sym_push 等）与 ncc 重名被 ELF 符号插值劫持 → `make libtcc.so LDFLAGS="-fPIC -Wl,-Bsymbolic"` 重建。ir-native 在 Linux ELF 运行时崩溃（2.0 预览），测试框架非 Windows 跳过（类比 p0_link）
- [x] **PA-10 可用性语义（已于 2026-08-06 完成）**：新增 native_memory_available()（Windows 0 / 其他 1），run_mode 分支改用它替代 #ifdef
- [x] **PA-11 tcc 目录探测重复**：native.c 与 xmake.lua 各一套，易漂移（已记录；建议后续以 NIHAO_TCC_DIR 为唯一来源）
- [x] **PA-12 A 方案 1.0 发布（2026-08-31 完成）**：门禁验证 ✅（c/native 各 12P/0F/5S + examples 6/6，Windows）；Linux 实测 ✅（PA-9，c/native 各 12P/0F/6S + examples 6/6 + `-run` 修复）；README 更新 ✅（安装/CLI/后端表 1.0/2.0 范围）；BNF v2.0 终校 ✅（`=>`/`->`/`T*` 补全，中英文档同步）；CHANGELOG.md 建立 ✅；本地 `v1.0.0` tag ✅（commit `2036fba`，合 main + push 由 ltree 决定，2026-08-31 已授权执行）。发布后 PA 分支进入冻结维护态
- [x] **PA-13 `is` 移除 `=>` 箭头形式（2026-09-15 完成，v1.0.2 收口）**：文档已删除单语句 `=>` 形式，C 后端 parser.c parse_is_stmt 的 TOK_FAT_ARROW 分支移除；IR 前端 irparse.c 的同分支于 2026-09-19 一并移除（双前端与 BNF v2.2 一致，错误路径吞 token 防死循环），只保留块形式 `is <pattern> { ... }`。测试文件 ir_is.nc 同步清理 `=>` 用例。token.h 注释改为"`=>` 词法保留、语法不使用"。属规范合规，允许在冻结线执行
- [x] **PA-14 清理废弃死代码（2026-09-15 起，2026-09-17 全链收官，冻结例外·代码卫生）**：parser.c 的 `parse_statement_full` / `parse_function_full` / `compile_file_full`（共 199 行）已废弃且无调用点，删除。ncc.h 对应声明同步移除。2026-09-17 续：删除早期直出 C 后端 `ncc/codegen.c`（507 行，已被 cgen.c + parser.c 管线取代）及其调用链——linker.c 120 行、ncc.h 84 行、ncc.c 4 行注册、xmake.lua add_files 条目
- [x] **PA-15 `is _` 通配符补全（2026-09-19 完成，v1.0.2）**：BNF v2.2 `<pattern>` 首项即通配符 `_`，但 1.0 线 A 后端 `parse_is_stmt` 未处理（`_` 被当作普通标识符按值比较，符号表无 `_` 时行为不确定），IR 前端 `irparse.c` 同样缺失（PB 线 parser.c 已实现，属分支漂移）。补全方式与 PB 一致：`_` 分支提前返回，A 后端 emits `if (1)` 恒真、IR 前端跳过比较与 JZ，块体仍走原有 `{ ... }` 入口；非块形式报错。`tests/pos/pattern.nc` 新增 `is _` 用例并断言输出（`wild ok` / `pattern ok`），c/native/ir-c/ir-native 四后端输出一致。属规范合规（文档已承诺该模式），允许在冻结线执行
- [x] **PA-16 `is` 多子句 fallthrough 缺口（2026-09-20 完成，v1.0.2，A 后端）**：ltree 决策「只修 A 后端，IR 留 2.0」。原缺口：BNF/中英 §6.1 规定多个 `is-clause`「按源码顺序求值，首个匹配者执行（无 fallthrough）」，而 A 后端为每个子句生成并列独立 `if`，条件重叠时多个子句都会执行（`is _` 恒匹配使该问题更易触发）。实现方式（不改动控制流骨架）：`TOK_WHILE` 的代码生成在 `int __is_val;` 旁加 `int __is_matched = 0;`（parser.c:1431，位于 `for (;;)` 外层块，故嵌套 `while` 各自持有独立标记），每轮迭代在 `if (!__is_val) break;` 之后清零（parser.c:1441）；`parse_is_stmt` 的每个子句守卫改为 `if (!__is_matched && <pattern> && (__is_matched = 1))`（parser.c:1332、1340），即「未匹配过 + 本模式命中」才进入块，进入时置位标记使后续子句全部短路。置位写在条件里而非块内首句，是因为块体由 `parse_statement` 整体输出，无法在其 `{` 之后插入语句。前子句不匹配时后续子句照常执行，与规范一致。测试：新增 `tests/pos/is_no_fallthrough.nc` + `.expect`（`is 3` 与 `is 1..5` 在 `n=3` 处重叠，只输出 `only three`；其余取值由 `is 1..5` / `is _` 接手，故同时覆盖标记的每轮重置）。未列入 `IR_SUBSET`：IR 前端（2.0 预览）按决策保持并列 `if`，该用例在 ir-c / ir-native 自动跳过。文档：中英 §6.1 实现状态注改写（A 后端已实现首匹配即止，IR 后端属 2.0 待对齐）、BNF §6 说明的「匹配顺序」条补落地口径、`IMPLEMENTATION_STATUS.md` 的 fallthrough 行由 ⚠️ 未实现转 ✅（A 后端）+ PB 差异行改为「1.0 已实现、PB 两前端待回灌」。门禁 c/native 各 **29P/0F/5S**、ir-c/ir-native 各 **6P/0F/24S**，0 FAIL，examples 6/7，既有 `pattern.nc` / `ir_is.nc`（单子句循环）行为不变。**待回灌 PB**：PB 的 A 后端与 IR 前端均为并列 `if`，可直接移植 `__is_matched` 方案，`is_no_fallthrough.nc` 作回归用例。**属规范合规（补齐规范已承诺的语义），允许在冻结线执行**
- [x] **PA-17 共享文档 PA↔PB 双向同步（2026-09-19 完成）**：以 `merge-base 7d4c652` 对 9 个共享文档做 `git merge-file` 三方合并（17 处冲突），PA 侧 v1.0.1/v1.0.2 成果进 PB（PB `37a112a`）；PB 侧更新的 4 项回灌 PA——中英 §5.1 指针声明定案表述、BNF `<array-size>` 细目与动态数组写法说明、VERSIONING_ROADMAP 的 2.0 进展条目与 `codegen.c` 清单残留、IMPLEMENTATION_STATUS「2.0 线（PB）差异」节 + `pattern.nc` 覆盖修正；`docs/LEGACY_CODEGEN.md` 纳入共享集。`TODO-PA.md`/`TODO-PB.md` 为分支专属不同步
- [x] **PA-18 循环体外 `is` 前端守卫（2026-09-19 完成，v1.0.2）**：BNF §6 规定 `is` 仅配合 `while` 循环体，但 A 后端把 `TOK_IS` 挂在 `parse_statement` 通用语句分支，循环体外写 `is` 前端照收、错误延迟到 C 编译期由 tcc 报 `'__is_val' undeclared`（`do` 体内同样静默接受）。实现方式：`CompilerState` 加 `while_depth`，`TOK_WHILE` 体前递增、体后递减，`case TOK_IS` 在 `while_depth == 0` 时报 `'is' pattern match only valid inside while loop body` 并吞 token 防死循环；IR 前端 `is_val_vreg < 0` 守卫保持不变（两侧口径统一）。新增 `tests/err/is_outside_while.nc` + `.expect`；门禁 c/native 各 13P/0F/5S（原 12P + 本用例）。副带效应：`do` 体内的 `is` 在 A 后端也即时报错（原为 C 编译期未定义符号），与 `do` 不支持 `is` 的规范一致。属规范合规，允许在冻结线执行。待回灌 PB 线 A 后端（见 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」）
- [x] **PA-19 `do` 体内 `is` 在 IR 前端拒绝（2026-09-19 完成，v1.0.2）**：PA 线 `irparse.c` 的 `TOK_WHILE || TOK_DO` 分支无条件写 `is_val_vreg`（残留 `is_do` 变量算了不用），使 `do cond { is pat { ... } }` 在 ir-c / ir-native 下合法，与 BNF §6 / 中英 §6.1「`do` 不支持 `is`」相反。改为 `do` 分支置 `is_val_vreg = -1` 并删除残留 `(void)is_do`，体内 `is` 走 `is_val_vreg < 0` 守卫报错（错误文案去掉 `while/do` 中的 `/do`，与 A 后端 PA-18 文案一致）；等价于 PB-27.1 的 IR 侧修复。配套测试框架收紧：err 用例改为仅 `IR_ERR_SKIP`（`m2a`~`m2d` M2 四条）在 IR 后端跳过，其余 err 用例四后端均执行；新增 `tests/err/is_in_do_body.nc` + `.expect`。门禁 c/native 各 14P/0F/5S、ir-c/ir-native 各 3P/0F/12S，0 FAIL。属规范合规，允许在冻结线执行。同项续做（A3 决策）：A 后端 `case TOK_DO` 进入体前把 `while_depth` 清零、退出恢复，使嵌套于 `while` 内的 `do` 也即时拒绝 `is`（原会静默匹配外层 `__is_val`），与 IR 前端一致，`tests/err/is_in_do_body.nc` 增补该嵌套情形；并修正「`do` 不支持 `is`」的理由句——BNF §6 与中英 §6.1 原写「因为 `do` 先执行块再判断条件」，与实现相反（本语言 `do` 与 `while` 同为前测循环），改为陈述为规范规定（`is` 只绑定 `while`，`do` 是否纳入留待 2.0 定案）
- [x] **PA-20 `?=` 安全赋值记号从语法移除（2026-09-19 完成，v1.0.2）**：ltree 决策「`=` 等价 `?=`，默认就是安全赋值检查的 → 去掉 `?=` 语法」。实现侧：`parser.c` 的 `parse_assign` 不再让 `?=` 与 `=` 共用分支，新增 `case TOK_SAFE_ASSIGN` 即时报 `'?=' is not part of the grammar; use '=' (every assignment is checked for visibility compatibility)`；`is_expr_continuer` 与语句窥探只认 `TOK_ASSIGN`；`token.h:112` 注释改为词法保留。词法 `TOK_SAFE_ASSIGN` 保留（与 `=>` 同处理），IR 前端自始无该分支故行为已正确。文档侧：BNF 升 **v2.3**（§1.4 运算符、`<assign-op>`、说明、附录四处）、中英 §5.1 多级指针示例、中英 §12 设计说明、两份语法元素表。新增 `tests/err/safe_assign_removed.nc` + `.expect`（`.expect` 仅取 `'?='` 子串以同时匹配双前端文案）。门禁 c/native 各 **15P/0F/5S**、ir-c/ir-native 各 **4P/0F/12S**，0 FAIL。属规范合规（消除文档承诺与实现的偏差），允许在冻结线执行。**待回灌 PB**：PB `parser.c` 三处（声明 498、语句窥探 1544、赋值 2505）仍把 `?=` 当 `=` 接受，`token.h` 注释亦为旧文案
- [x] **PA-21 `?.` / `?(` 安全解引用记号从语法移除，检查落入 `.()`（2026-09-19 完成，v1.0.2）**：ltree 决策「在 `.()` 语法里做真的可见性/边界检查，去掉 `?.` `?()` 等附加语法，功能都加、语法收聚」。实现侧：① `parser.c` 的 `parse_postfix` 新增 `TOK_SAFE_DOT` 显式拒绝分支（报 `'?.' is not part of the grammar; use '.()' (dereference performs the visibility and bounds checks)`），`is_expr_continuer` 与解引用窥探去掉 `TOK_SAFE_DOT`；`irparse.c` 同位置加同文案 `ir: '?.' ...` 分支，双前端一致（`?(` 从未是 token，词法切成 `?` + `(`）。② `parse_deref_chain` 补编译期宽度检查：读侧 `vis_check_usable` + 写穿侧 `vis_check_writable` 已有，新增 `.(T)` 的 `sizeof(T)` 与指针静态所指字节数比较，超出即报错；字节数由 `malloc(T)`/`malloc(T,n)` 初值记入新的 `Symbol.pointee_bytes`（`ncc.h`），或经 `&x` 由 `sym->type->ref->size` 推得；对指针本身重新赋值时清零（`ParserState.lhs_was_deref` 区分 `p.(T) = v` 与 `p = v`），保守不检查。③ 运行期 `NULL` 检查明确不属本项（非空由 §5.1 静态规则保证，`malloc` 返回值检查留 2.0），文档按此改写。文档侧：BNF v2.3（§1.4 运算符终端、`<postfix-op>`、说明、附录）、中英 §5.1 示例与 §14.1 整节、两份语法元素表、英文运算符清单。新增 `tests/err/safe_dot_removed.nc`、`tests/err/deref_bounds.nc`（后者依赖 `.(T)`，IR 前端只有裸 `p.()`，故加入 `IR_ERR_SKIP`）。struct/array/宽分配探针确认无误报。门禁 c/native 各 **17P/0F/5S**、ir-c/ir-native 各 **5P/0F/13S**，0 FAIL，examples 6/7。属规范合规 + 检查补全，允许在冻结线执行。**待回灌 PB**：PB 两前端仍把 `?.` 当 `.(` 别名，且无 `.(T)` 宽度检查
- [x] **PA-22 从属查询与位域偏移内置函数补实现（2026-09-20 完成，v1.0.2）**：ltree 决策「补漏，中英保持一致，补实现到 A 后端」+「改三参 `structof(T, member, ptr)`」。中英 §2.3 自始列出 `structof` / `unionof` / `holdof` / `bitoffsetof`，但 A 后端 `parse_primary` 只把 `sizeof`/`typeof`/`alignof`/`offsetof`/`visof` 交给 `parse_builtin_kw`，四个关键字报 unexpected token。实现：① 新增聚合体成员查找辅助 `agg_member_sym`，`parse_builtin_kw` 补三参从属查询分支，生成 `((void*)((char*)(ptr) - offsetof(T, m)))`；诊断覆盖「T 非聚合体」「structof 用于 union」「unionof 用于 struct」「m 不是 T 的成员」「参数个数/逗号不符」；`holdof` 对 struct/union 通用。② `bitoffsetof(T, m)` 定为声明序位偏移：以前置最近非位域成员为锚点，用 C 的 `offsetof`/`sizeof` 求单元起点（对齐以基类型 `sizeof` 近似——tcc 无 `_Alignof`），加整单元数与单元内位数；目标非位域、基类型宽度未知、同组存储类型不一致报错。③ 二参签名不可实现是定稿三参的理由：仅凭成员地址无法得知该成员在聚合体内的偏移。文档：BNF 升 **v2.4**（`<builtin-call>` 三参 + `bitoffsetof` 语义注释）、中英 §2.3 定稿 + 版本口径注、中英 §5.2 示例改为可编译写法、两份语法元素表四行补签名。测试：新增 `tests/pos/ownerof.nc`（struct/union 反推 + 位域 8/12/64）与 `tests/err/structof_bad_member.nc`；IR 前端不改动（2.0 布局待做），前者不入 `IR_SUBSET` 自动跳过、后者入 `IR_ERR_SKIP`（跳过提示文案改为「IR 前端未实现该检查/语法」）。门禁 c/native 各 **19P/0F/5S**、ir-c/ir-native 各 **5P/0F/15S**，0 FAIL。属规范合规（补回文档承诺的能力），允许在冻结线执行。**待回灌 PB**：PB 两前端均未接入这四个关键字
- [x] **PA-23 结构体成员列表分隔符：文档示例与实现不一致（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「只改 4 处文档示例（最小改动）」。中英文档 4 处（§7.4.1 与 §14.1 各一处，中英各半）把成员写成 `Person struct { name char[], age i32 }`，而 BNF `<type-decl> ::= <identifier> "struct" "{" { <field-decl> } "}"` 没有分隔符终端，双前端实测也一律拒绝逗号（A 后端 `expected member name, ','`、IR 前端 `ir: expected member name`）。BNF 与实现本就一致，`ncc/tests`、`ncc/examples` 亦零处使用逗号，故仅把 4 处示例改为空白分隔，不改 BNF、不改实现、不加注释与用例。探针确认改后形态在 c / native 下编译运行正常（`print("age=%d\n", p.age)` 输出 `age=3`）。**探针另发现两项 IR 前端（2.0 线）缺口，已登记 `IMPLEMENTATION_STATUS.md`「2.0 线（PB）差异」**：① IR 前端拒绝任何数组类型成员（`char[8]` 与省略长度的 `char[]` 均报 `ir: expected member name`），标量成员正常；② IR 前端未内建 `print`（直出成未知函数，链接期 `undefined symbol 'print'`），IR 侧只能用 `puts`——这也解释了 `IR_SUBSET` 用例一律用 `puts`。1.0 线不受影响（A 后端两项均正常），不属本项范围
- [x] **PA-30 `print` 语义在规范中定义（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「文档定义 printf 直通 + 示例改格式串（推荐）」，并批准同轮发现的「整型喂给 `puts`」一并改写。实现事实（探针复核，A 后端 `parser.c:2349–2368`）：`print` 有**两种形态**，按首实参静态区分——① 首实参是字符串字面量 → 整条转发为 C `printf(...)`，第一参数即格式串，多余参数不自动拼接、不自动换行（故 `print("[LOG] ", msg)` 只输出 `[LOG] `，`msg` 被丢弃）；② 首实参不是字面量 → 生成 `printf("%lld\n", (long long)expr)`，即按十进制整数打印该值并换行（`print(p.(i32))` 打印所指整数值；`print(s)` 若 `s` 是字符串变量则打印其地址整数）。IR 前端完全没有 `print`（直出未定义符号，链接期报错），跨后端用例只能用 `puts`。文档改动（中英同步）：新增 **§2.3.1 输出内建（`print` / `puts`）** 三条口径 + 两形态区分注 + 版本口径注（`print` 属 A 后端、`ir-*` 未内建）；§7.4.1 的 `print("[LOG] ", msg)` 改为 `print("[LOG] %s\n", msg)`；§14.2 的 5 处 `puts(整型)`（`puts(val.(i32))`、`puts(q.(i32)) // 输出 200`、`puts(p2.(i32)) // 100`、`puts(m.(i32)) // 900` 与 §12.1 分析表同行）改为 `print(...)` 整型形态，`puts("Inspecting value: ")` 改 `print(...)` 以免插入换行。测试：新增 `tests/pos/print_forms.nc` + `.expect`（3 行断言：格式串形态打印 `char[]`、`print("...")` 不带换行紧跟 `print(整型)`、`flow`+`malloc` 后经 `const` 参数打印所指值），未列入 `IR_SUBSET`（IR 无 `print`，自动跳过）。门禁 c/native 各 **28P/0F/5S**、ir-c/ir-native 各 **6P/0F/23S**，0 FAIL，examples 6/7。同轮把中英 §14.2 整段示例抽出编译复验：除 `flow new_owner void = ptr` 一行外全部通过，该行缺陷另立 PA-33。**待回灌 PB**：PB 文档无 §2.3.1 口径，PB `parser.c` 的 `print` 分支形态需按本口径核对，`print_forms.nc` 可作 2.0 接入 `print` 时的回归用例。**属规范合规（补回文档缺失的语义定义），允许在冻结线执行**
- [x] **PA-24 `alignof` 在 `c` 后端不可用（2026-09-20 完成，v1.0.2）**：ltree 决策「ncc 编译期自行算对齐，输出字面量」（未选推荐的 `__alignof__` 方案）。原缺陷：`parse_builtin_kw` 与标识符内置函数表两处生成 `_Alignof(T)`，libtcc 0.9.27 不提供该符号，`c` / `native` 后端链接期报 `undefined symbol '_Alignof'`，而中英 §2.3 与 BNF `<builtin-call>` 都把 `alignof` 列为可用。实现：新增 `type_align()`（type.c:71–102，ncc.h:400 原型），编译期递归求对齐——数组/别名经 `ref` 取元素对齐、`struct`/`union` 取最宽非位域成员（递归，深度上限 16）、`void` 按 §5.1 通用指针计 8、其余取 `CType.align`，为 0 时回落 `type_default_align(kind)`（`parse_type` 在栈上零初始化 `CType`，标量分支不填 `align`，故必须回落）。两处分派改为 `cgen_raw("%u", type_align(&tmp))`（parser.c:1788、2279），生成的 C 只剩整数字面量。文档：中英 §2.3 的 `alignof` 条目补「编译期常量 + 三类取值口径 + `void` 计 8」、BNF `<builtin-call>` 的 `alignof` 产生式加同口径注释（产生式未变，不升版本）、`IMPLEMENTATION_STATUS.md` 的 `alignof` 行由 ⚠️ 有缺陷转 ✅ 并新增「2.0 线（PB）差异」行（PB `parser.c` 仍 `_Alignof`、PB `irparse.c` 对任何 `alignof` 固定返回 8）。测试：新增 `tests/pos/alignof_builtin.nc`（9 项断言：`u8`/`i32`/`f64`/`void`/`struct`/嵌套 `struct`/`union`/`i32[4]`/`char[8]`，用 `puts` 以避开 IR 侧无 `print` 的限制），未列入 `IR_SUBSET`。门禁 c/native 各 **25P/0F/5S**、ir-c/ir-native 各 **6P/0F/20S**，0 FAIL。同项顺带将 PA-23 遗留的「聚合体成员分隔符」状态行由 ⚠️ 文档示例不一致改为 ✅（该决策本轮已闭环）。**待回灌 PB**（PB 两前端均未接入编译期对齐）
- [x] **PA-25 指针后缀链与切片补实现（2026-09-20 完成，v1.0.2）**：ltree 决策「补实现：`.(` 进通用 postfix 链 + 空 `[]` 与切片读」，收尾两项子决策——通用 `void` 指针裸下标「报错 + 文档改写法」、切片赋值「一并实现逐元素写回」。中英 §5.1.2 / §5.1.3 的全部写法此前在 A 后端编译不过：解引用被锁在独立的 `parse_deref_chain`，`.(T)` 之后无法再接下标/成员/切片。实现：① 删除 `parse_deref_chain`，`.(T)` / `.()` 成为 `parse_postfix` 链上的普通算子（`deref_step` / `deref_prefix`，用 `cgen_mark` + `cgen_take_prefix` 把已生成的前缀整体包裹，包裹时补回被截掉的行首缩进）；数组类型转成合法的 `T (*)(N)`（`c_cast_name`）。② `p[]` 空下标 = 省略类型的 `p.()`（§5.1.1 三级指针 `p[][].(i32)`）。③ 切片读 `p[a..b]` → `&p[a]`（PA-27 起：上下界同为字面量时另记录 `hi-lo` 供 `len()` 取用，见下）；`[..b]` 等价 `[0..b]`；`T[n] x = p[a..b]` 按声明容量 `memcpy` 复制。④ 切片写 `p[a..b] = {v…}` → 逗号表达式逐元素写回。⑤ `T[n] x = malloc(T[n])` 声明退化为 `T (*x)[余维]`（`c_decay_suffix`），`malloc` 字节数 = 元素个数 × 最深元素 `sizeof`（供 `.(T)` 宽度检查）。⑥ 通用 `void` 指针裸下标/切片前端即时报错并提示 `p.(T)[i]`。⑦ 修 `c_type_suffix` 多维只输出最后一维。文档：BNF **v2.5**、中英 §5.1.2 / §5.1.3 示例改写为可编译形态并加定案注（含 §5.1.3 原 `.(int64)` 报错注释改为「槽位宽度静态不可知 → 跳过检查」的正确口径）。测试：新增 `tests/pos/deref_slice.nc`、`tests/err/void_subscript.nc`（入 `IR_ERR_SKIP`）。门禁 c/native 各 **21P/0F/5S**、ir-c/ir-native 各 **5P/0F/17S**，0 FAIL。IR 前端不改动（2.0 待做），实现待回灌 PB。**属规范合规（补回文档承诺的写法），允许在冻结线执行**
- [x] **PA-27 `len(x)` 内置函数补实现（2026-09-20 完成，v1.0.2）**：ltree 决策「补实现三类全」。中英 §2.3 自始列出 `len(x)`（数组=容量 / 动态字符串=字面量长度 / 切片=边界差），A 后端却完全没有该内置函数（`parse_primary` 把 `len` 当普通标识符，符号表无 `len` 时报未定义）。实现：① `Symbol` 加 `len_known` / `logical_len`，在**声明期**登记——数组取 `type_array_count`（多维为各维乘积）；`char[] s = "..."` 这类未定长数组/字符串取字面量长度；推断的切片声明取 `hi-lo`。② `len` 接入 `parse_primary` 的标识符内置函数表（实参必须是标识符），静态不可知时前端即时报 `len: logical length of 'X' is not statically known`，保守而非返回过期值。③ 对裸标识符整体重新赋值时撤销登记（`ParserState.lhs_bare_ident` 区分 `a[0] = 1` 与 `a = ...`）。④ 推断的切片声明 `s = p[a..b]` 改为**指针视图**代码生成（`E *s = &p[a]`，`s.()` 即首元素），修掉 PA-25 遗留缺陷（旧实现按 `hi` 分配并 `memcpy`，会读到切片右界之外）；`T[n] x = p[a..b]` 仍按声明容量复制。文档：BNF 升 **v2.6**（`len` 产生式注释三类取值 + 版本口径）、中英 §2.3 补 `len` 定稿与注、中英 §5.1.2 定案注改写为「记录 `b-a` 供 `len()`」口径、`IMPLEMENTATION_STATUS` 新增 `len` 行与「2.0 线差异」行。测试：新增 `tests/pos/len_builtin.nc`（8 项断言，多维/动态字符串/字面量/闭区间/左开区间/复制/重赋值后稳定）与 `tests/err/len_unknown.nc`（入 `IR_ERR_SKIP`：IR 前端静态不可知时返回 0 而非报错）。门禁 c/native 各 **23P/0F/5S**、ir-c/ir-native 各 **5P/0F/19S**，0 FAIL，跨后端输出一致，examples 6/7。IR 前端不改动（三类取值已有，报错口径待 2.0 对齐），实现待回灌 PB。**属规范合规（补回文档承诺的内置函数），允许在冻结线执行**
- [x] **PA-28 `else if` 在 IR 前端补支持（2026-09-20 完成，v1.0.2）**：ltree 决策「补实现：IR 前端支持 `else if`」。BNF `<if-stmt> ::= "if" <expr> <block> [ "else" ( <if-stmt> | <block> ) ]` 与中英 §6/§13 示例一贯包含 `else if`，A 后端 `parse_if_stmt`（`while (cur_tok == TOK_ELSE)` 循环）也支持，唯 IR 前端在 `else` 之后无条件调 `ir_block`，`else if` 直接报 `expected '{', got 'if'`。实现：把 IR 前端的 if 语句抽成 `ir_if_stmt`，`else` 后 peek 到 `if` 时递归自身、否则照旧走块，多级分支共用同一条 `JZ`/`JMP` 标签链（无新增 IR 指令）。测试：新增 `tests/pos/elseif_chain.nc`（链中命中 / 中间命中且末尾无 `else` / 全不命中 / 循环体内 / `else` 普通块，带 `.expect`）并入 `IR_SUBSET`，c/native/ir-c/ir-native 四后端输出一致。文档：BNF v2.6 版本行追加记录该口径（语法本身未变）、`IMPLEMENTATION_STATUS.md` 新增「条件与分支（§6）」节两行（`else if` 双前端一致；`else` 非块在 A 后端因代码生成缺分隔符而延迟失败，有效语义仍与 BNF 一致）、PB 差异表加 `else if` 行（PB `irparse.c` 仍缺）。门禁 c/native 各 **24P/0F/5S**、ir-c/ir-native 各 **6P/0F/19S**，0 FAIL，examples 6/7。**待回灌 PB**（PB `irparse.c` 的 `else` 分支同样强制块）**属规范合规（补回文档承诺的写法），允许在冻结线执行**
- [x] **PA-29 §7.3 调用方接收规则与 §12.1 传递矩阵收口（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「以 §7.3 为准，只改文档」。矛盾点：§7.3 规定 `flow` 返回值只能由 `flow` 接收（禁止列含 `const`），而 §12.1 矩阵明确允许 `flow → const`（只读借用）与 `flow → var`（可变借用）；`const` 行禁止列漏 `var`（§12.1 禁止 `const → var`）；三行把 `func` 当作可禁止的接收属性列出，而 §12.1 规定 `func` 不是变量存储期、不参与矩阵；`IMPLEMENTATION_STATUS.md` 无 §7.3 行且 PB 差异行引用了不存在的「§12.3 后半」。定案理由：两者约束对象不同——§12.1 是**同一作用域内已存在变量之间**的赋值（源变量仍在，借用结束可解冻或由 owner 释放），返回值是临时量，用 `var`/`const` 接收 `flow` 返回值同时造成无人释放与悬空，故 §7.3 有意更严格。文档改动（中英同步）：§7.3 三行禁止列改为纯存储期属性并给 `const` 行补 `var`、加 `func` 不能作接收属性注、新增「为什么本表比 §12.1 更严格」论证段 + 三条推论 + `const q void = create_buffer(1024)` 反例；§12.1 的 `func` 说明限定矩阵适用范围并回指 §7.3；中英 §7.3 加实现状态注（1.0 不强制执行 / 2.0 PB-29 已实现 / 冻结线不移植）。`IMPLEMENTATION_STATUS.md` 新增「调用方接收规则（§7.3）」三行表（全部 ⚠️ 未实现）、§12.1 节标题限定为"仅限同一作用域内变量之间的赋值"、PB 差异行的「§12.3 后半」改为 §7.3。探针复验：`const q void = create_buf(4)`、`var r void = create_buf(4)`、`flow t void = get_ver()` 在 c / native 均静默编译并运行，故状态记为"未实现"而非"已实现"。门禁、代码与测试均不变（文档-only）。**待回灌 PB**：PB 的 §7.3 表仍是旧版（含 `func` 禁止列、`flow → const` 冲突），共享文档下次同步时按本口径并入
- [x] **PA-26 §5.1.4 复合体指针示例全量补实现（2026-09-20 完成，v1.0.2）**：ltree 决策「修 a+b+c，d 另登记（推荐）」。范围（a/b/c 三项，均为中英 §5.1.4 / §5.1.5 示例编译不过的缺口）：① 切片赋值的字符串右值 `p[a..b] = "abc"`：`parse_assign` 切片分支在值列表之外接受字符串字面量，按**字节**整段 `memcpy`（含结尾 `\0`）；闭区间右界 `b` 即最后一个可写字节，故字面量长度须 `<= b-a`，放不下时前端即时报 `string needs N bytes with terminator, slice holds M`（`ParserState.slice_len_known` / `slice_len`）。② 推断声明取成员类型 `name = xiaoming.name`：`infer_init_type`（parser.c:592–645）的标识符分支补两 token 前瞻（用 `LexerState` 副本做非破坏式深层前瞻），识别 `x.m` 并取成员类型；数组成员按 C 规则退化为指针（`char[9]` → `char*`）；聚合体成员保留标签符号，避免 `c_type_name` 把变量名当类型名输出。③ 推断声明取调用返回类型 `result = calc(10, 20)`：同函数内识别 `f(x)` / `cb(x)`，RHS 为 `TYPE_FUNC` 时取其 `next`（返回类型）而非函数类型，函数指针声明 `calc void(i32, i32) i32 = add` 与调用推断随之可用。④ §5.1.4 的 `stptr.(Say).say = "..."` 经 PA-25 的通用后缀链已可用，本轮复验通过。文档：BNF 升 **v2.7**（`<assign-stmt>` 补切片右值两式注释、新增推断声明说明、版本头定案文案，IR 前端标注 2.0 待做）、中英 §5.1.1 隐式推断条目扩展、中英 §5.1.2 新增字符串右值切片赋值条目、中英 §5.1.4 新增「推断声明取成员类型」注、`IMPLEMENTATION_STATUS.md` 行号全量重基（+3/+17/+46/+64 映射）并新增切片写/推断声明两行与「2.0 线（PB）差异」行。测试：新增 `tests/pos/slice_str_infer.nc`（5 项断言：切片字符串写、偏移写、动态字符串成员赋值 + `xiaoming.say`/`xiaoming.name` 推断、函数指针赋值与调用推断）与 `tests/err/slice_str_overflow.nc`（入 `IR_ERR_SKIP`，IR 前端无切片赋值路径）。门禁 c/native 各 **27P/0F/5S**、ir-c/ir-native 各 **6P/0F/22S**，0 FAIL，examples 6/7。**待回灌 PB**：PB `parser.c` 完全没有切片赋值分支（无 `rhs_was_slice`），`infer_init_type`（PB parser.c:576）标识符分支仍 `memcpy(out, s->type)` + `out->sym = s`，成员访问与调用两种右值均取错类型。**属规范合规（补回文档承诺的写法），允许在冻结线执行**
- [x] **PA-31 `T[n] x = "literal"` 数组声明初始化：声明容量被丢弃、无容量诊断（2026-09-20 完成，v1.0.2）**：ltree 决策「① 补实现：数组存储 + 容量诊断」。原缺口：`ch char[9] = "xiaoming"` 直出 `char(*ch) = "xiaoming";`——声明容量完全丢失、退化成指针声明器，且 `char[4] = "xiaoming"` 静默通过，与 PA-26 切片写入口径不一致。实现（`parse_declaration`，parser.c:1159–1174）：① 判定初值形态——当前 token 是字符串字面量且声明类型是**已知容量**的数组（`type_array_count > 0`）时，不再走「取 RHS 前缀 + 指针退化」路径，`rhs_text` 保持 `NULL`，声明头走普通数组形态（parser.c:1212–1216）生成 `char ch[9]`，初值由后续 `= "..."` 原样交给 C 做数组初始化。② 两条前端诊断：元素类型非 `char`（`type_deepest_elem(...)->kind != TYPE_CHAR`）报 `cannot initialize array 'X' with a string literal; its elements are not 'char', use a value list {v0, v1, ...}`；`strlen(字面量) + 1 > 容量` 报 `string needs N bytes with terminator, array 'X' holds M`（与 PA-26 切片写同文案前缀，闭区间口径一致）。③ 省略容量的 `char[] s = "..."`（TYPE_STRING）不受影响，仍是指针形态动态字符串，`len()` 取字面量长度；定长数组的 `len()` 按 §2.3 取声明容量。④ `T[n] x = malloc(T[n])`（PA-25 ⑤）的退化判定走 `else if` 分支，行为不变，`examples/05_string.nc` 的 `char s[6] = "hello"` 现生成真正的数组存储。测试：新增 `tests/pos/str_array_init.nc`（数组存储 + 原地改写 + `len()` 取容量 + `strlen+1 == n` 边界）与 `tests/err/str_array_overflow.nc`、`tests/err/str_array_bad_elem.nc`（后两条入 `IR_ERR_SKIP`）。文档：中英 §5.1.2 定案注各加两条目（语义 + 实现口径）、BNF 升 **v2.8**（产生式不变，§2 `<array-type>` 说明定案该语义）、`IMPLEMENTATION_STATUS.md` 新增状态行 + 测试覆盖三行 + PB 差异行，PA 侧 `parser.c` 行号全量重基（+17）。门禁 c/native 各 **32P/0F/5S**、ir-c/ir-native 各 **6P/0F/27S**，0 FAIL，examples 6/7。**待回灌 PB**：PB 两前端均无容量检查与非 `char` 拒绝。**属规范合规（补齐 §5.1.1 / §2.3 承诺的数组语义），允许在冻结线执行**
- [x] **PA-32 `flow` 绑定字符串字面量 / `&x` → 退出时 `free()` 非堆存储（2026-09-20 完成，v1.0.2）**：ltree 决策「② 登记存储来源，free 只发给堆右值」。原缺陷：中英 §11.1 / §12.1 允许 `flow` 接收指针且规定块退出时自动释放，A 后端也确实无条件为块内 `flow` 变量在退出点生成 `free()`（`parser.c:1644–1656`），没有区分「堆所有权」与「只读字面量 / 他方存储」。探针事实（`c` / `native` 双后端一致）：`flow s char[] = "xiaoming"` 直出 `char* s = "xiaoming"; … free(s);`，运行期对静态只读段执行 `free`，实测该用例无任何输出（测试框架捕获到空 stdout 判 FAIL）；同一体内改 `const` 则正常。PA-30 复核另发现同一类的第二条路径：`flow p void = &variable` 生成 `void* p = &variable; … free(p);`，且**函数级** `flow` 同样在函数退出时释放（`parser.c:1285–1292`），即释放栈地址。实现：① `Symbol` 新增 `no_auto_free`（`ncc.h`，零初始化为 0 → 默认保持既有释放行为，只有明确登记为非堆来源者被豁免）。② `parse_declaration` 在初值形态判定处按右值首 token 登记来源（parser.c:1131–1139）：`TOK_STRING_LITERAL`（静态只读段）、`TOK_BITWISE_AND`（`&x`，指向栈帧/静态段/他方对象）、`TOK_LBRACE`（聚合存储）→ `no_auto_free = 1`，其余（`malloc` 系、函数返回等）→ 0。③ `parse_assign` 的 `TOK_ASSIGN` 分支在 `=` 之后、右值解析之前，对**裸标识符 LHS** 的 `flow` 变量重新登记（parser.c:2915–2923），即整变量重绑定按新右值改判来源；`p.(T) = v`、`p[i] = v` 因 `lhs_bare_ident` 为假不动标记。④ 两处自动释放点（函数级 parser.c:1285–1292、块退出 parser.c:1644–1656）的条件各加 `&& !s->no_auto_free`。已知代价（按决策②刻意保留）：`flow q void = malloc(i32)` 后 `q = &n` 重绑定会放弃旧堆块的所有权而不在重绑定点插入释放，旧块泄漏——保守选择，避免经别名双释放；精确回收属 2.0 所有权转移分析。测试：新增 `tests/pos/flow_no_free.nc` + `.expect`（字面量 `flow`、`&n` 取址 `flow`、`malloc` 堆 `flow`、堆→栈重绑定四形态，断言输出完整；修复前任一非堆形态都会中止运行期）。未列入 `IR_SUBSET`：IR 前端不做所有权/自动释放（2.0 线），该用例在 ir-c / ir-native 自动跳过。文档：中英 §11.1 新增「`flow` 的自动释放只发给堆所有权」四行右值来源表 + 重绑定与泄漏口径；中英 §14.1 的 `flow person_ptr void = &some_person` 补注（该形态不自动释放）；`IMPLEMENTATION_STATUS.md` 的 ⚠️ 行转 ✅、块退出行补来源判定、新增测试覆盖行与「2.0 线（PB）差异」行，PA 侧 `parser.c` 行号重基（+9 / +18）。门禁 c/native 各 **33P/0F/5S**、ir-c/ir-native 各 **6P/0F/28S**，0 FAIL，examples 6/7。**待回灌 PB**：PB `parser.c` 无来源登记，`flow` 绑定字面量/取址仍会生成非法 `free`，`flow_no_free.nc` 可作回归用例。**属运行期安全缺陷修复（消除对静态/栈存储的非法释放），允许在冻结线执行**
- [x] **PA-33 文档承诺的 `flow → flow` 所有权转移在 1.0 线无法书写（2026-09-20 完成，v1.0.2）**：ltree 决策「② 局部豁免本语句自身源」（未选推荐的①调整检查顺序）。§12.1 传递矩阵明确允许「`flow` 源 → `flow` 目的 = 所有权转移，源失效」，中英 §14.2 的 `flow new_owner void = ptr` 即该形态示例。原缺陷（A 后端，双后端一致）：`vis_check_assign`（`vis.c:158–218`）在**检查阶段**就调用 `vis_update_source` 把源标记为 `BS_INVALID`（`vis.c:96–98`），而右值标识符随后才在表达式生成时经 `vis_check_usable`（`parser.c:2226`）读取，于是同一条语句必然报 `'ptr' is invalidated: its ownership has been moved`；两条路径同时命中——`flow` 声明初始化（`parser.c:1146–1158`）与普通赋值（`parser.c:2902–2924`），即 `flow → flow` 在 1.0 线**完全不可写**；借用形态 `flow → var` / `flow → const` 不受影响（源转 `BS_FROZEN`）。实现（按决策②，不动检查顺序）：① `ParserState` 加 `moved_src`（ncc.h:244），`vis_check_assign` 在置 `BS_INVALID` 的同时登记该符号（`vis.c:205–211`），`vis_check_usable` 仅对**当前语句自身**的转移源放行（`vis.c:134`），下一条语句进入 `parse_statement` 即清除（`parser.c:1393–1395`）——「失效」因此自下一条语句起生效，与文档口径一致；② 转移同时把源标记 `ownership_transferred`（`vis.c:205–211`），使失效源不再参与 §11.1 的自动释放，释放责任移交接收方（否则同一堆对象被 `free` 两次），实测转移链只生成一处 `free`；③ 同轮复验 §14.2 分析表（Chinese.md:1528 / English.md:1554）发现的相邻缺口一并修复：源处于冻结（`BS_FROZEN`，已被 `const`/`var` 借用）时转交所有权此前**静默通过**，现即时报 `cannot transfer ownership of 'ptr': it is borrowed (frozen) by an active const/var`（`vis.c:194–201`）——否则新所有者会在借用者仍指向同一对象时释放它。文档：中英 §12.1 状态说明新增三条口径（失效的生效边界 / 冻结源不可转交 / 转移与自动释放的责任移交），中英 §11.1 来源表新增「已转移出去的源」一行并说明转移不属泄漏，中英 §14.2 的 `flow new_owner void = ptr` 行内注释改为「错误：源正被借用」（此前该行注释写成「ptr,read_ref,mut_ref 失效」，与其下方分析表的「检查失败」自相矛盾）；BNF 升 **v2.9**（产生式不变，§11/§12 处新增 `flow → flow` 实现口径注，并标注 IR 前端不做所有权/借用检查）、`IMPLEMENTATION_STATUS.md` 把该行由 ⚠️ 检查顺序缺陷转 ✅ 并补三条测试覆盖行与「2.0 线（PB）差异」新增行，PA 侧 `parser.c` 行号全量重基（+3）。测试：新增 `tests/pos/flow_move.nc`（声明式转移 + 赋值式转移 + `&x` 来源被转移后重新登记为堆所有权）与 `tests/err/flow_move_frozen.nc`（入 `IR_ERR_SKIP`）。门禁 c/native 各 **35P/0F/5S**、ir-c/ir-native 各 **6P/0F/30S**，0 FAIL，examples 6/7。**待回灌 PB**：PB `vis.c` 无 `moved_src` 与冻结源守卫，两种写法在 PB 线仍自报失效。**属规范承诺（补回文档承诺的所有权转移形态），允许在冻结线执行**
- [x] **PA-34 多变量声明的定长数组后缀被静默丢弃（2026-09-20 完成，v1.0.2）**：ltree 决策「① 多变量分支复用 PA-31 判定」。原缺陷（PA-31 同轮复核发现）：`parse_declaration` 的多变量分支（`var {aa = "aa",…} char[2]`，中英 §4.2 示例）只输出 `c_type_name(&bt)` + 变量名、不接 `c_type_suffix(&bt)`，声明的数组后缀整体丢弃，字面量被赋给标量 `char`，tcc 仅告警、ncc 零诊断，且 `len(aa)` 报「静态不可知」（PA-27/PA-31 的登记只覆盖单变量路径）。实现：`arr_cap = type_array_count(&bt)`（parser.c:815）判定已知容量后——① 声明器输出补数组后缀（853–855）使每个变量得到真正的数组存储 `char aa[3]`；② 逐变量复用 PA-31 的两条诊断（超容量 `string needs N bytes with terminator, array 'aa' holds M`、元素非 `char` 要求改值列表，835–852）；③ 登记 `len_known`/`logical_len`（869–878），省略容量的 `{…} char[]` 一并登记字面量长度（探针确认该形态此前就正常：退化为 `char* aaa = "aaa"`）。新增第三条诊断：定长数组类型的多变量声明遇非字符串字面量初值（如 `{x = 1,y = 2} i32[4]`）报 `has fixed array type but no string literal initializer; use a single-variable declaration for this form`——该形态此前静默生成错误 C，单变量分支的对应路径（取 RHS 前缀 + 指针退化）依赖整段重排，不在多变量分支复制。**副带效应**：①落地后中英 §4.2 原示例 `char[2]` 自身超容量（`"aa"` + NUL = 3 > 2），故示例改为 `char[3]` 并加口径注释；②多变量 + 定长数组的非字面量初值从「静默错误」变为即时报错。文档：BNF 升 **v2.10**（§4 说明新增定长数组口径条，产生式不变）、中英 §4.2 示例改 `char[3]`、中英 §5.1.2 定案注各加「多变量声明的同形态」条目并把实现口径改为 PA-31/PA-34、`IMPLEMENTATION_STATUS.md` 该行由 ⚠️ 缺陷转 ✅ 并新增覆盖行与「2.0 线（PB）差异」未回灌行、`parser.c` 行号全量重基（多变量分支净增 36 行）。测试：新增 `tests/pos/multi_arr_decl.nc`（定长数组存储 + `len()` 取容量 + 动态 `char[]` 取字面量长 + 标量形态不受影响）与 `tests/err/multi_arr_overflow.nc`、`tests/err/multi_arr_nostr.nc`（三条均入 `IR_ERR_SKIP`/不入 `IR_SUBSET`，IR 前端不支持该形态）。门禁 c/native 各 **38P/0F/5S**、ir-c/ir-native 各 **6P/0F/33S**，0 FAIL，examples 6/7。**待回灌 PB**：PB `parser.c` 的多变量分支与 PA 修复前完全相同（探针确认），可直接移植本实现，三条用例作回归。**属规范合规（补齐 §4.2 示例承诺的形态并与 §5.1.2 定案对齐），允许在冻结线执行**
- [x] **PA-35（C1）编译器版本号两套值：`NIHAO_VERSION` 0.1.0 与 `set_version` 1.0.2 冲突（2026-09-20 完成，v1.0.2）**：ltree 决策「**xmake 注入做单一真源**」+ 附属项「`nihao.toml` 的 `[project] version` **跟随 `NIHAO_VERSION`**」。原矛盾：`ncc.h` 的 `NIHAO_VERSION` 写死 `"0.1.0"`，用户可见的三处输出（`--version`、帮助横幅、`-v` verbose 启动行）全部打印 `v0.1.0`，而 `xmake.lua` 的 `set_version("1.0.2")` 与 CHANGELOG/ROADMAP/GIT_CONVENTIONS 的 v1.0.2 发布口径直接冲突。实现：① `xmake.lua` 顶部新增 `local nihao_version = "1.0.2"`，`set_version(nihao_version)` 与 `add_defines("NIHAO_VERSION_TAG=" .. nihao_version)` 同源于此**一处**（实测 `get_config("version")` 在本工程解析期返回 nil，故用局部变量而非回读配置）；② `ncc.h` 撤掉独立版本常量，改为两级字符串化宏 `NIHAO_STR`/`NIHAO_STR_` 把注入的裸 token（`1.0.2`）转成字符串，未注入时（LEGACY `Makefile` 直编，该入口已标注弃用）落到占位值 `"0.0.0-unknown"` 并注释说明它不代表任何对外发布口径；③ `ncc.c` 的 `nihao init` 生成 `nihao.toml` 时 `[project] version` 由字面量 `0.1.0` 改为编译期拼接 `NIHAO_VERSION`。实测：`ncc --version` 与帮助横幅输出 `NihaoC Compiler v1.0.2`，`init` 产出的 `nihao.toml` 为 `version = "1.0.2"`。文档：`VERSIONING_ROADMAP.md` §3.2 补「版本号单一真源」条款（发布时只改 `xmake.lua` 一处 + 占位值口径 + 脚手架版本取编译器版本），`GIT_CONVENTIONS.md` §1「版本化与发布」加同一必查项，`CHANGELOG.md` v1.0.2 段登记。测试：`ncc/tests` 无版本字符串断言，门禁不受影响，复验 c/native 各 **38P/0F/5S**、ir-c/ir-native 各 **6P/0F/33S**，0 FAIL。**待回灌 PB**：PB `ncc.h`/`xmake.lua` 仍是两套值（PB 侧 `set_version` 为 2.0 口径），移植时按同一 stringize 方案改并把局部变量置为 PB 的发布版本。**属代码卫生（消除同义两值），允许在冻结线执行**
- [x] **PA-36（C2）README 的现在时计数与实测不符（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「换为现值 + 加权威指针」。原矛盾：README「1.0 范围」条写「全量语法回归 24P/0F，examples 6/6 通过」，而 v1.0.2 实测为每后端 **38P/0F/5S**、examples **6/7**（复现：`examples/*.nc` 逐个 c 后端编译并运行，`06_cooking` 失败于 `cooking const: expected '='`（parser 不认 `cooking` 块内的编译期函数形态），`ir-c` 编译通过并产出可执行文件）。修改：① 该条改述为「v1.0.2 门禁实测：全量语法回归每后端 38 PASS / 0 FAIL / 5 SKIP，examples 6/7」；② 补一句失败原因与后端差异（`06_cooking.nc` 属 2.0 预览示例，`cooking` 编译期块与编译期函数尚未纳入 1.0 语法，`ir-c`/`ir-native` 可通过），避免读者把 6/7 当成缺陷；③ 加**权威指针**：「计数随版本变化，以 `CHANGELOG.md` 当前版本段与 `docs/IMPLEMENTATION_STATUS.md` 为准」，防止再次漂移；④ 相邻的「双平台验证（2026-08-31）」条保留 `examples 6/6` 原文但补「v1.0.0 时点」「当时的用例集」定语，明确其为历史快照而非现态。**同类过时计数登记于 C4（`STAGE_SUMMARY.md`）与 C5（`ROADMAP.md` 的 PB 验收行），本项不并入**（各自决策口径不同）。文档-only 变更，无代码与测试改动；门禁基线不变。
- [x] **PA-37（C3）ROADMAP 架构图含幽灵目录与错误的层级（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「重写为真实目录树」。§二「当前代码架构」原图三处失实：① `test/`、`arch/`（"目录已建，内容为空"）、`myapp/` 三个路径在仓库中根本不存在（`git ls-tree PA/PB` 与工作区双重确认）；② 把 `前端` / `后端` 画成子目录，而 `ncc/` 实际是全部 `.c/.h` 扁平放置；③ 手工示例位早在仓库根 `examples/`，图里仍挂在 `ncc/test/`。另缺 `ir_backend.h`、`build/`、`ncc/.gitignore`，且 `Makefile` 仍与 `xmake.lua` 并列称"双构建系统"（该文件顶部已标 LEGACY 弃用）。重写：仓库根 → `docs/` → `examples/` → `ncc/` 的真实层级，`ncc/` 下按 `[前端] / [前端·A 线] / [前端·IR 线] / [后端]` 行内注释区分职责而不再画子目录，补 `ir_backend.h`、`build/`（注明由 `ncc/.gitignore` 忽略）、`editors/sublime/` 实际内容，`Makefile` 标 LEGACY、`xmake.lua` 标"版本号单一真源"（与 PA-35 口径一致）。PA 与 PB 两分支的 `ncc/` 根文件清单经 diff 确认逐字相同，故本图为共享文档跨分支通用。**幽灵项在 README/docs 其余文件中无引用（全仓 grep 确认），故仅本文件改动**。文档-only，门禁基线不变。
- [x] **PA-38（C4）STAGE_SUMMARY.md 以现在时陈述已过时的现状（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「标为历史快照 + 权威指针」（未选刷新为 v1.0.2 现值、未选拆成纯归档）。原矛盾：文件标题写「2026-08-30 / 更新至 2026-09-01」的阶段快照，正文却是现在时现状文档——§2「全量 parser 测试 12P+4S」、§3「IR 用例 31 个」、§4「c/native 12P、IR 一致性 26P」「**当前** 56 PASS」、§3.1「当前语法以 `BNF.md` **v2.2** 为准」（实为 v2.10），且第 3 行把「读本文件」当作会话恢复入口，会让读者把 8 月口径当成 v1.0.2 现值。处理：① 文件头加显著横幅——本文件为历史快照、定格于 2026-09-01，现状权威口径改指 `CHANGELOG.md` 当前版本段 + `IMPLEMENTATION_STATUS.md` + `BNF.md` 最新版 + `ROADMAP.md`；恢复入口改为 `README.md` + `CHANGELOG.md` + `TODO.md`；并说明本文件的两项不可复得价值（§3.1 覆盖矩阵是 `PB24_DECISION.md` 引用的决策原始依据、§3「关键坑」清单其余文档无载），故保留不动；② §2/§3/§4/§5 标题加「快照至 2026-09-01」定语，四处现在时计数各补一句 v1.0.2 现值指针（c/native 各 38P/0F/5S、IR 线各 6P/0F/33S），§4 的命令行注释去计数、另立一条对比「快照计数 vs 现值」；③ §3.1 快照注补第三轮收敛（`?.` / `?(` 移除、检查落入 `.()`／PA-21），并把「以 BNF v2.2 为准」改为「以 BNF 最新版为准（v1.0.2 时为 v2.10）」；④ §1 两条随双仓库合并过时：「内部活跃仓库 `D:\workspace\workbudy\ncc` + 发布副本随 GitHub 推送同步」「tar 同步到另仓各自 commit（wincred 可直推）」标注为**已废止流程**，改写为现行做法（单仓库 `PA`/`PB` 分支 + 共享文档 `git merge-file` 三方合并；推送与 tag 由所有者逐次授权、不自动执行），避免 wincred 那句与 GIT_CONVENTIONS 的禁止自动推送规则并读时误导；⑤ §5 清单加盘点快照注，就地澄清三条已完成/重复项（PA-9 已实测完成、link `-l` 传递早已实现且清单未去重、中英规范已核对至 BNF v2.10）。连带 README 顶部导航把该链接标签由「进度总结」改为「阶段总结（历史快照）」，与横幅口径一致。文档-only，无代码与测试改动；门禁基线不变。**PB 线待回灌本文件（若两分支版本有差异）**。
- [x] **PA-39（C5）ROADMAP 里程碑表的 PB 验收行挂着无法溯源的详细计数（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「只保留 0 FAIL + 日期 + 指向 TODO-PB」+ 附属项「定规则：表内只写不变量」。原矛盾：`docs/ROADMAP.md` 里程碑表「验收 | PB `xmake test --all` 全矩阵 73 PASS / 0 FAIL / 26 SKIP」一行① 无日期，② `73`/`26` 在 PB 分支 `docs/TODO-PB.md` 中找不到对应记录（PB 侧只记「全矩阵 0 FAIL」与分阶段数字），③ 本行为跨分支共享文档却写单分支口径，PA 分支既无法实测也无从更新——属 README（PA-36）、STAGE_SUMMARY（PA-38）同类的"把单分支某时点详细计数写进共享文档"模式。修改：① 该行换为不变量口径「**0 FAIL**（PB-25 ~ PB-29 阶段验收，2026-09-04 ~ 09-16；PASS/SKIP 详细计数随 PB 线用例增删波动，权威口径见 PB 分支 `docs/TODO-PB.md`）」；② 里程碑表上方新增「本表计数写法约定」三条（表内只写不随用例增删漂移的不变量且必带日期／详细 PASS/FAIL/SKIP 计数写分支专属 TODO／仅发布行可写门禁计数且必须绑定版本号，权威详值指 `CHANGELOG.md` 对应版本段），并注明约定自 2026-08-29 分支化之后的行起适用，骨架期各行按当时口径原样保留；③ 据此把本文件 v1.0.2 发布行的「门禁 c/native 各 38P/0F/5S…」绑定版本号改写为「v1.0.2 门禁 …（权威详值见 `CHANGELOG.md` v1.0.2 段）」，文件头更新日期上抬至 2026-09-20；④ 同类规则升入跨分支规则文档：`GIT_CONVENTIONS.md` §1 新增「共享文档的测试计数写法」一条（共享层只写不变量＋版本/日期，详细计数入分支专属 TODO 与 CHANGELOG；快照文档须标快照日期＋权威指针、不得以现在时陈述现状）。文档-only，无代码与测试改动；门禁基线不变。**PB 线待回灌**（`ROADMAP.md` 与 `GIT_CONVENTIONS.md` 均为共享文档）。
- [x] **PA-40（C6）GIT_CONVENTIONS.md 的失实/失效陈述（分支基线、双仓库义务、失效引用）（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「三项全部就地修正」+ 附属项「不加防漂移约定，仅修正事实」。逐项核实与修改：① §一「分支规则」原第 1 条「三条分支均从同一稳定点分出（**当前基于** `main` 的 `04f1435`）」为现在时失实——`04f1435` 是 2026-08-29 分支化当时的 `main`（实测该 commit 存在，日期相符），而 `main` 现已前进到 `90c1742`（2026-09-19），`PA` 为 `dc2dac0`（2026-09-20）；另 `git merge-base PA PB` 实测为 `7d4c652`（2026-08-31），即 PA/PB 的实际共同祖先晚于分支化点，"均从同一稳定点分出"已不成立。改为带日期的历史陈述并补实际共同祖先。② §一「版本化与发布」共享文件条末尾「双仓库（NihaoC ↔ ncc 活跃）**必须成对提交**防基线漂移」与 PA-38 刚定案的「双仓库同步流程已废止」（`docs/STAGE_SUMMARY.md` §1）直接冲突，且本文件是约束后续工作的规则文档，保留会让读者继续按两套仓库操作。改为：双仓库成对提交流程**已废止**（标 PA-38 并指回 STAGE_SUMMARY §1），两条线由同仓库 `PA`/`PB` 分支承载，共享文档跨分支用 `git merge-file` 三方合并同步；「语法演进先 PB → PA、bug 修复先 PA → PB」的既有口径不动。③ §五 里程碑两行「（见 §1 发布门禁）」为失效交叉引用——本文件 §1 只有「分支规则」「版本化与发布」两个小节，无「发布门禁」，推送硬门禁实际在 §3.1「硬性约束：禁止自动推送」。两处改为「（推送授权门禁见 §3.1）」。④ 同类失效引用在 `docs/TODO-PA.md` 的 PA-35 记录中发现一处（「`GIT_CONVENTIONS.md` §1 发布门禁加同一必查项」），按同一口径改为 §1「版本化与发布」。**未选**：在「分支规则」补一条「commit 锚点只作历史快照、必带日期、不写现在时」的防漂移约定（ltree 决策仅修正事实；同类防治已由 PA-39 的「共享文档的测试计数写法」规则覆盖）。事实核实手段：`git rev-parse`/`git log -1 --date=short` 取 `PA`/`main` 现值、`git merge-base PA PB`、`git log -1 04f1435`；`§1 发布门禁` 残留以全仓 grep 确认清零。文档-only，无代码与测试改动；门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7）。**PB 线待回灌**（`GIT_CONVENTIONS.md` 为共享文档；PB 侧①的日期口径相同，但②中 PB 才是双仓库历史的实际执行方，回灌时按同一废止口径处理）。
- [x] **PA-41（C6b）VERSIONING_ROADMAP.md 仍以现在时规定已废止的双仓库流程（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「原文保留 + 废止注」（未选改写为现行单仓库流程、未选整篇标为历史规划快照）+ 计数项「绑定日期保留原数字」（未选换为 PA-39 的不变量口径）。缺陷：本文件制定于 2026-08-30 且无更新标注，§3.3「同步手段：现有 `cp + 双仓库双提交` 流程（ncc 活跃仓库 ↔ NihaoC）」、§3.4 整节「双仓库映射」（含「1.0 期间新增工作流：PA 的共享文件改动需 cp 到 ncc 活跃仓库」「PB 线活跃开发继续走 ncc（feat/backend-ir），周期性 cp 回 NihaoC/PB 提交」）、§3.6 风险表「共享文件改动必须双仓库同步提交」三处以现在时规定 PA-38 已定案废止、PA-40 刚从规则文档撤下的流程，与 `GIT_CONVENTIONS.md` §1 并读自相矛盾；另有 §3.3 流程图 PB 行与 §3.5 门禁表 PB 行两处「56 PASS」单分支某时点计数无日期锚。处理（原叙述文字一律不改）：① §3.3 同步手段行末尾追加 **【2026-09-20 废止】** 注，指明现行做法（单仓库 `PA`/`PB` 分支 + 共享文档 `git merge-file` 三方合并）与权威指针（`GIT_CONVENTIONS.md` §1）；② §3.4 节标题下方插入废止横幅（本节整体废止、原文保留作历史记录，并说明现行合并基准 `7d4c652`、推送与 tag 由所有者逐次授权 §3.1、下表与下两条仅描述本文件制定时（2026-08-30）的状态），两条工作流 bullet 各加「（已废止）」；③ §3.6 对策行保留原文并补括注（同步手段已改为单仓库分支 + 三方合并，见 §3.4 废止注）；④ 两处 `56 PASS` 各绑定「2026-08-30 制定时用例集」定语（§3.3 流程图行同时把「基线」改为该日期口径，§3.5 保留「或合入时当时基线」）；⑤ 文件头由单行「制定日期：2026-08-30」扩为「制定日期 ｜ 最近更新：2026-09-20（C6b/PA-41 摘要）」。**同类计数已核不必改**：`docs/PB24_DECISION.md` §2.2「测试矩阵（56 PASS / 0 FAIL）」——该文件标题即带「（2026-08-30，决策条件已满足）」，整篇为决策记录，按 PA-39 规则属已绑定日期的历史记录。文档-only，无代码与测试改动；门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7）。**PB 线待回灌**（`VERSIONING_ROADMAP.md` 为共享文档）。
- [x] **PA-42（C7）中英规范的重复小节编号与标题层级错位（2026-09-20 完成，v1.0.2，文档-only）**：ltree 决策「标题编号＋层级一起修」+「中英同步改」（未选只改编号、未选把 §12.2 升为第 15 章）。缺陷（两文件行号完全对称）：① §6 控制结构内出现**两个 `### 6.1`**——「6.1 条件语句」（Chinese.md:497 / English.md:521）与「6.1 模式匹配（`is` 子句）」（Chinese.md:561 / English.md:585），后者排在 `### 6.2 循环结构` 之后，编号应为 6.3；② 「12.2 函数参数与返回值的传递规则」（Chinese.md:1245 / English.md:1269）用 h2（与 `## 12.` 平级）而编号是 12.x，其下 `### 参数传递`／`### 返回值`（English `### Parameters`／`### Return values`）层级随之错位。修改：① 模式匹配节改 `### 6.3`；② §12.2 降为 `### 12.2`，其下两子节降为 `####`；③ 引用面核查——`docs/IMPLEMENTATION_STATUS.md:116` 的表标题「`is` 模式匹配（§6.1，BNF v2.2）」随编号改为 §6.3 并注明「规范标题自 PA-42 起编号」；`docs/BNF.md` 两处只写 `§6`（第 337、446 行）不受影响；`§12.2` 的其余引用（Chinese.md:1146 / English.md:1170 的正文交叉引用，及 `IMPLEMENTATION_STATUS.md:27 / 198 / 199` 三行）因编号未变继续有效。**同类已核不改**：`docs/Chinese.md:1161` 与 `English.md:1185` 的 `#### 综合传递矩阵 / #### Combined Transfer Matrix` 与其上一级 `### 12.1 指针传递总矩阵` 语义重复，属正文措辞而非编号/层级缺陷，未纳入本项。文档-only，无代码与测试改动；门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7）。**PB 线待回灌**（`Chinese.md` / `English.md` / `IMPLEMENTATION_STATUS.md` 均为共享文档，中英两份在本轮改动后标题行仍逐行对应）。
- [x] **PA-43（C8）语法元素表「其他运算符」小节残缺＋英文表三行 bullet 标记畸形（2026-09-21 完成，v1.0.2，文档-only）**：ltree 决策「5 行全补＋标使用状态」+「英文表残缺 bullet 一并修」（未选只补已用的 4 行不列 `::`、未选删掉空小节让 BNF 单独承载）。缺陷：① `docs/NihaoC 语言关键字与语法元素表格.md:237`「其他运算符」下仅一个空 bullet（原 239 行裸 `- `），`docs/NihaoC Language Keyword and Grammatical Element Table.md:142`「Other Operators」同样只有裸 `-`——实现与 BNF 已承认的记号在表内无行：`..`（切片区间，仅出现在 `[start..end]` 组合行）、`...`（动态数组标记，BNF `<array-size>` 四种形态）、`;` 与 `#`（语句终止符，BNF `<empty-stmt> ::= ";" | "#"` 与 §1.4 说明）、`::`（`token.h:163` 有词法，`lexer.c` 识别，但 `parser.c` / `irparse.c` 消费次数均为 0，即词法保留、语法不使用）；② 英文表 75-77 行是 `- - \`long\` …` 畸形双 bullet 标记（中文表对应 129-131 正常），中英计数不等。修改：两份各补 5 行（措辞与 BNF §1.4 / `<array-size>` 对齐，`::` 明示「词法保留、当前语法不使用」），中文表末尾裸 `- ` 一并去掉，英文表三行改回单 bullet 标记并与中文表逐行对应。**验证手段**：两份 bullet 行计数均为 120（改动前中 116 / 英 115，其中中文那 116 含末尾裸 `- `），`grep -cE '^- *$'` 两份均为 0，`grep "^- - "` 全 docs 无残留。文档-only，无代码与测试改动；门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7）。**顺带澄清 D 组**：本轮 `git diff PA PB` 核到 PA 的两份语法元素表**并无 `=>` / `?=` 行**（PA-13 / PA-20 已随正文删除），`=>` 行与 `?=` 行只存在于 PB 副本，且 PB 的 `=>` 行文字已自述「已于 2.0 移除（PB-27.6）」——故「语法元素表 `=>` 行残留」在 1.0 线无对象，D 组该项转为「PB 副本两行的归属与回灌判定」。另注：这两份表在 PA/PB 间存在描述性漂移（PA 带三参签名与可见性检查措辞，PB 未回灌），属共享文档同步范围。**PB 线待回灌**（两份语法元素表）。
- [x] **PA-44（C9）`ncc/vis.c` 注释把 §12.1 的传递矩阵写成 §12.2（2026-09-21 完成，v1.0.2，注释-only）**：ltree 决策「两处都改」+「全矩阵重跑门禁后再提交」（未选只改 68 行、未选反向改规范文档注明 §12.2 由本文件承担、未选只跑 c/native 或免跑）。缺陷：① `vis.c:68` `vis_check_transfer()` 的注释写「ch.12.2 matrix」，而规范中带「矩阵」字样的小节只有 **§12.1 指针传递总矩阵**（`Chinese.md:1157`，英文规范对应同名节），§12.2 是「函数参数与返回值的传递规则」（`Chinese.md:1245`）——该函数体逐条实现的正是 const→仅 const、static→const/static、flow→const/flow/var、var→const/var 四行矩阵；② `vis.c:6` 文件头「State machine for pointer transfer rules (ch.12.1 / 12.2)」把 §12.2 一并算进本文件，但本文件全部入口只有 `vis_check_assign()` 及其下游（同作用域赋值路径），**§12.2 的参数/返回值路径在 1.0 线没有实现代码**（与 PA-29 定案一致：该检查 1.0 未实现、仅文档注明），属超范围声明。修改（**严格保持行数不变**，避免 `IMPLEMENTATION_STATUS.md:14-20` 七行 `vis.c:55–218` 行号引用失效）：68 行改 `ch.12.1 matrix`；6 行单行改为 `ch.12.1 matrix; ch.12.2 function/return rules unimplemented in 1.0`，同时去掉原 `:` 前误挂的 12.2。**同轮全量复核其余章节引用，实测均正确无需改动**：`§11.1`（parser.c:1131 / 2915、ncc.h:130、vis.c:208）、`§12.1`（parser.c:1256 / 2512 / 2928、vis.c:88、ncc.h:252、token.h:112 / 164）、`ch.13.1`（parser.c:1661、vis.c:116 / 214）、`§14.2`（vis.c:194）、章级 `ch.11-14`（vis.c:4）。验证：`git diff --numstat` = 2 加 2 删、无行位移；`xmake` 重建 build ok；`xmake test --all` **2026-09-21 复跑与 v1.0.2 基线逐项一致**——c / native 各 **38P/0F/5S**、ir-c / ir-native 各 **6P/0F/33S**，0 FAIL。注释-only，无逻辑与用例改动。**一致性核对 A/B/C 三组至此全部收口**。**PB 线待回灌**（`ncc/vis.c` 的注释在两线同源；PB 若已有 §12.2 实现，回灌时按 PB 实际路径改写该行而非照抄「unimplemented in 1.0」）。
- [x] **PA-45（D1）语法元素表中/英两表的 `=>` / `?=` 行判为分支差异并结案，PB 待回灌清单登记（2026-09-21 完成，v1.0.2，文档-only）**：ltree 决策「判为分支差异，各自口径 + 登记待回灌」（未选本轮切 PB 分支回灌、未选把 PB 两行删成 PA 口径）。核查手段：`git diff PA PB -- <两份表>` 全量比对 + `git -c core.quotepath=false show PB:ncc/token.h` + PB 侧 `BNF.md` 逐行 grep。事实：① PA 两份表**均无** `=>` / `?=` 行（随 PA-13 移除 `is ... =>` 单语句形式、PA-20 移除 `?=` 一并删）；② PB 两份表**保留**两行且与 PB 自身实现一致——`token.h:111` `TOK_FAT_ARROW`（表内自注「已于 2.0 移除(PB-27.6)」，仅词法保留）、`token.h:112` `TOK_SAFE_ASSIGN`、PB `docs/BNF.md:69 / 82 / 203 / 422` 仍把 `?=` 列入词法表与赋值运算符。**结论**：两行不构成文档与实现不一致，而是 1.0 冻结线与 2.0 线的实现差异；若按 PA 口径强删，反而制造 PB 侧新的不一致（PB 编译器仍能解析 `?=`）。**执行**：PA 两份表保持现状，本轮不切 PB 分支、不改 PB 任何文件。**PB 待回灌清单**（下一轮在 PB 分支逐条处理，条目均来自上述 diff）：① `bitoffsetof` / `holdof` 两行补参数签名与语义（`bitoffsetof(type, member)` 声明序位布局＋编译期常量、`holdof(type, member, ptr)` struct/union 通用；PA-22 定稿口径）；② `structof` / `unionof` 两行补三参签名与返回类型（`structof(type, member, ptr)` 返回 `void` 指针、`unionof(...)` 仅接受 `union`；同 PA-22）；③ `=` 行补「每次赋值都执行可见性兼容性检查，即原安全赋值语义」定语（PA-20／B1 定案）；④ `.()` 行补「自带可见性检查」，`.()`／`.(type)` 由 PB 现文本「内存越界检查」补为「可见性检查＋内存越界检查」（PA-21 落点）；⑤ 英文表 `long` / `float` / `double` 三行畸形 `- - ` bullet（PA-43 已修，PB 未修）；⑥ 两份表「其他运算符」节 PB 仍是一行裸 `- `（中文表）/ 裸 `-`（英文表，且缺文件末尾换行），PA 已补 `..` / `...` / `;` / `#` / `::` 五行（PA-43）；⑦ 英文表 PB 缺文件末尾换行；⑧ 计数核对：当前 PA 中/英各 120，PB 中 118 / 英 117（中文表含 1 行裸 `- ` 占位），回灌 ①~⑦ 且按本判定保留 PB 的 `?=` / `=>` 两行后，PB 中/英应各为 **122 = PA 120 + 2**（回灌时以实测复核，**PB 比 PA 多两行属预期，不再按「与 PA 归一」处理**）。PB 侧其余既有待回灌项见 `CHANGELOG.md` v1.0.2 段各条末「PB 线待回灌」标注，本项不重复列举。文档-only，无代码与测试改动；门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7）。**PB 线待回灌**（本条判定与其 ①~⑧ 清单需在 PB 分支执行；本项对 `CHANGELOG.md` / `ROADMAP.md` / `GIT_CONVENTIONS.md` 的登记同为共享文档，随回灌轮一并三方合并）。**遗留 D 组由 3 项降为 2 项**（D2 `main` 落后 PA 的镜像陈述、D3 v1.0.2 tag 与推送）。

  > **2026-09-21 语法设计固定轮追记（PA-48）**：本条的计数基线已变。本轮把 `short` / `int` / `long` / `float` / `double` 五个 C 风格别名从类型清单移除并合并为一行词法保留条目（BNF v2.11 §1.2 `<reserved-keyword>` 扩为 8 个），两份表因此各减 4 行：**PA 中/英现各 116 bullet**。据此 PB 回灌后的期望值由 122 改为 **118 = PA 116 + 2**（仍多 `=>` / `?=` 两行，属预期分支差异）。回灌清单 ①~⑦ 与判定本身不变。
>   **2026-09-21 同轮 PB 回灌执行完毕（追记）**：`PB` 分支已按三方合并（merge-base `7d4c652`）把 ①~⑦ 全部落地，两份表实测中/英各 **118 bullet** = 本条修正后的期望值 `PA 116 + 2`；剩余差异仅 `?=` / `=>` 两行，PB 侧登记为 `TODO-PB.md` **PB-31**（随该轮 P1 记号移除后删除、归一 116）与 **PB-33(P1)**。本条「下一轮在 PB 分支逐条处理」的部分即此完成，D1 判定维持结案。
- [x] **PA-46（D2）`main` 镜像陈述无日期且与仓库实际不符——改为可核对陈述并完成 PA→main 本地合入（2026-09-21 完成，v1.0.2，文档 + 本地合入，无推送）**：ltree 决策「本地合入 main + 给陈述绑日期」+「现在就合」（未选只改文档让 main 继续落后、未选再加防漂移规则、未选等 D3 一并合）。缺陷：`docs/ROADMAP.md` v1.0.2 发布行与 `docs/GIT_CONVENTIONS.md` §五 里程碑行均以现在时写「`main` 已重新对齐为 `PA` 镜像」，而实测 `main` = `90c1742`（2026-09-19）、`PA` 领先 **31 个提交**（`git rev-list --count main..PA`），且 `PA..main` 有 13 个 main 侧提交（均为历次「release: 合入 PA」合并与 TODO 分层等），现时态陈述不成立——与 PA-36／38／39 同属"某时点状态写成现在时、无日期锚"的漂移模式。**合入安全性前置核查**：`git diff --name-status main PA` 统计为 **52 个新增（A）+ 23 个修改（M）+ 0 个删除（D）**，即文件层面无 main 独有内容，合入不会丢改动。**执行**：① 两处陈述改为「已于 2026-09-21 由 `PA` 本地合入对齐」＋PA 侧内容锚点（PA-45 提交 `e0ecb28` 与本次文档提交）＋可复现核对命令 `git diff main PA`（结果应为空）；merge commit 不写死 sha（写作时它还不存在），改为指向 `git log main -1` 溯源；② 在 `main` 上 `git merge --no-ff PA`（提交信息带 `[AI]`，沿用既有「release: 合入 PA」样式），合入后跑 `git diff main PA` 验证为空；③ 本文件与 `CHANGELOG.md` 的遗留口径由「D 组 2 项」改为「仅剩 D3」。**边界**：仅本地分支拓扑操作——**未打 tag、未推送**（`origin/PA..PA` 与 `origin/main..main` 均有未推送提交，推送授权门禁见 `GIT_CONVENTIONS.md` §3.1）；`PB` 分支不受本项影响。无代码与测试改动，门禁基线不变（c/native 各 38P/0F/5S、ir-c/ir-native 各 6P/0F/33S、examples 6/7；本项未改 `ncc/` 任何源文件，故不重跑）。**PB 线待回灌**（`ROADMAP.md` / `GIT_CONVENTIONS.md` / `CHANGELOG.md` 为共享文档；main 合入只针对 1.0 线发布流程，PB 线不适用同一动作）。
- [x] **PA-47（D3）`v1.0.2` tag 落点与形态：本地附注 tag 打在 `PA` 已验证 tip，推送留给 ltree（2026-09-21 完成，v1.0.2，文档 + 本地 tag，无推送）**：ltree 两项决策——主项「**附注 tag，打在 `PA` 当前 tip `18f9c77`**」（未选打在含本条登记的最终提交、未选轻量 tag、未选暂不打 tag）、附属「**不推送（保持现状）**」（未选本轮推送分支或 tag）。执行与依据：① **落点溯源**——`git diff --stat 6b69cfb 18f9c77 -- ncc/ examples/` 输出为空，证明 PA-46 提交 `18f9c77` 与门禁复跑提交（PA-44 `6b69cfb`，2026-09-21 复跑 c/native 各 **38P/0F/5S**、ir-c/ir-native 各 **6P/0F/33S**、examples 6/7）代码与示例逐字节相同，中间提交只动文档，故 tag 无需上抬也不必重跑；② **形态核实**——`git tag -l --format='%(refname:short) %(objecttype)'` 实测 `v1.0.0` = tag（附注）、`v1.0.1` = commit（轻量），既有约定不统一，故本项取附注并把规则写回文档；③ **tag 命令** `git tag -a v1.0.2 18f9c77 -F <消息文件>`，消息正文含门禁基线、BNF 版本、PA-1 ~ PA-46 范围、版本号单一真源、未做项与「推送由 ltree 逐次授权」口径（含反引号文本一律走 `-F` 文件，不用 `-m`）；④ **规则落地** `docs/VERSIONING_ROADMAP.md` §3.2 新增「tag 形态」条目（后续发布统一附注 tag；`v1.0.1` 为历史轻量不回改；落点为发布线已验证 tip，合入 `main` 后可用 `git merge-base --is-ancestor <tag> main` 核对），同节 tag 台账由「v1.0.2 待 tag」改为 `v1.0.2` → `18f9c77`（2026-09-21，annotated，本地未推送）；⑤ **配套收口**——`CHANGELOG.md` v1.0.2 段标题日期改 2026-09-21 并标本地 tag，`ROADMAP.md` v1.0.2 行状态 `🟡 待 tag` → `✅ 已 tag（本地，推送待授权）`，本文件与两份规则文档的「遗留仅 D3」口径统一为「A/B/C/D 四组全部收口，唯一剩余动作是推送」。**边界**：本项不执行任何 `git push`（分支与 tag 均未推送，当前未推送量 `origin/PA..PA`、`origin/main..main`、`origin/PB..PB`，以及本地三个 tag（远端 tag 状态在授权推送前用 `git ls-remote --tags origin` 核对），见 §3.1 授权门禁）；tag 之后本条文档提交不进 `v1.0.2`。**PB 线待回灌**（`VERSIONING_ROADMAP.md` §3.2 的 tag 形态规则与台账、`CHANGELOG.md`/`ROADMAP.md`/`GIT_CONVENTIONS.md` 的 D3 结案口径均为共享文档；2.0 线的 `v2.0.0` tag 应按同一附注规则执行）。

- [x] **PA-48（语法设计固定轮 R1~R19）四份设计文档剥离实现状态、按裁定固定语法口径（2026-09-21 完成，v1.0.2，文档-only）**：ltree 决策「按推荐执行修复」——`docs/SYNTAX_DESIGN_REVIEW.md` §11 的 R1~R19 全部照建议落地，并规定「本轮只固定设计，代码缺口逐条登记」。执行内容：① `BNF.md` 升 **v2.11**，文件头新增分层声明（设计层文档零实现状态、跨分支逐字一致），版本历史表改写为纯设计条目；`<for-step>` 放宽为 `<expr>`（R12）、`switch` 各 case 不穿透且 1.x 无 `fallthrough`（R13）、`<statement>` 补 `<label-def>`（R18）、`is <identifier>` 定为按值比较（R14，绑定/解构留 2.0）、`#` 降级为兼容终止符（R1）、`register`/`restrict`/`volatile` 词法保留（R17）、内置名与关键字统一「不参与用户作用域解析」（R11）、`fx32`/`fx64` 定性为同宽整型存储且定点语义未定义（R4）、`[...]`/`[...N]` 标注兼容保留（R5/R6）、C 风格声明标注为兼容写法（R9）。② **R19 新增裁定**：`short`/`int`/`long`/`float`/`double` 从 `<primitive-type>` 移除，基本类型定为 **16 个**；五个别名转入 §1.2 `<reserved-keyword>`（词法保留数由 3 改为 **8**，与 `token.h:62-66` 的 `TOK_SHORT`~`TOK_DOUBLE` 现状一致）。实测依据：A 后端在类型位置逐个报 `unexpected token 'short' in expression`…；PA `parser.c` / `irparse.c` 与 PB `parser.c` 对这五个 token **零消费**，全部 `tests/` 与 `examples/` 无使用点。③ `Chinese.md` / `English.md` 同步落地并剥离实现状态（含 §3.1 被 blockquote 截断的基本类型表修复、§6.2 补齐 `switch` 小节、§6.3 模式表按值比较定案、§2.4 关键字计数改指 BNF §1.2、英文规范补 §2.4 关键字总指针），中英条目对齐；实现状态整体移入 `IMPLEMENTATION_STATUS.md`（新增「语法固定轮实测记录 F1~F9」与 §6 三行、`is <identifier>` 行改判为与规范一致）。④ 两份语法元素表：五个别名从类型节移入关键字节并合并为一行、`fx32`/`fx64` 描述补「1.x 按同宽整型存储」、`#` 改为「兼容语句终止符」、类型节补 16 个基本类型的指针说明——**中英 bullet 各由 120 变为 116**。核对命令：`grep -n "实现状态\|A 后端\|PA-[0-9]" docs/BNF.md docs/Chinese.md docs/English.md`（应只剩 2.0 定案性表述）。**门禁影响：纯文档，无代码变更，v1.0.2 基线计数不变。**本条同时更正 PA-45 的 PB 回灌期望值（见该条追记）。
>   **同轮两处口径追补（随本条一并结案）**：① `IMPLEMENTATION_STATUS.md`「语法固定轮实测记录」引言把探针样本范围写成 `probe1.nc ~ probe6.nc`，实际为 `probe1.nc ~ probe9.nc`（F8/F9 两条为后补），已更正并与该节末行一致；② 同文件「测试覆盖」末注原写「`is <identifier>` 变量绑定仍无用例（对应上表 ⚠️ 项）」，与本条把该行改判为 ✅「按值比较、与规范一致」矛盾，已改为「绑定与解构属 2.0 范围，故 1.x 无需该用例」。两处均为文档表述自洽修正，无状态变更。

- [ ] **PA-49（F1）结构体成员默认值的代码生成是坏的（2026-09-21 登记，待修复）**：`P struct { x i32 = 7  y i32 }` 前端接受，生成的 C 为 `typedef struct P { 7int32_t x; int32_t y; } P;`，tcc 报 `error: invalid number`（成员默认值被当作类型名前缀输出）。设计侧产生式 `<field-decl> ::= … [ "=" <expr> ]` 意图明确并已固定（BNF v2.11 §3.1），属实现未跟上。动作：A 后端补成员初值的正确 lowering（或先在前端给出「暂不支持」的即时诊断，避免生成非法 C）+ `tests/pos` / `tests/err` 用例；修复前该写法属「语法可写、不可编译」，已在 `IMPLEMENTATION_STATUS.md` F1 记录。

- [ ] **PA-50（F2）生成的 C 在字符串字面量里写入真实换行字节（2026-09-21 登记，可移植性缺陷，待修复）**：源写 `print("%d\n", i)`，A 后端产出的 C 里是 `printf("` + 0x0A + `", i)`，而非 `\n` 两字符。tcc 作为扩展容忍，故当前全部门禁（c/native 38P/0F/5S）都在 tcc 下过而不报错；一旦换 gcc/clang/MSVC 编译产物，或把 `.c` 交给外部工具链，全部用例会一起红。动作：字符串字面量 lowering 时按转义序列重新编码（`\n`/`\t`/`\r`/引号/反斜杠/不可打印字节），并补一条「产物 C 可被非 tcc 编译器解析」的抽查用例。见 `IMPLEMENTATION_STATUS.md` F2。

- [ ] **PA-51（F8/F9）`[...]` / `[N...]` 四档数组容量写法与 A 后端实际能力不符（2026-09-21 登记，待决策 + 修复）**：实测 A 后端（c/native）接受 `[N]`、`[...N]`、`[...]`，**拒绝**设计规范里作为动态数组规范写法的 `[N...]`（以及 `[N..]` / `[..N]` / `[..]` 三种点号写法）；且 `[...]` 在 A 后端是**退化为指针**，不存在设计承诺的「默认容量 8」（8 槽只成立于 IR 槽模型，见 `tests/pos/ir_slice.nc:11`）。连带缺陷：`a i32[...] = {1,2,3}` 生成 `int32_t* a = {1, 2, 3};`（非法 C，tcc 报 `'}' expected`）。两条动作待 ltree 定：① 要不要在 A 后端补 `[N...]` 词序并让 `[...]` 落到显式容量；② 还是把设计文字改窄（`[...]` 明写为「指针退化」、`[N...]` 待实现）。本轮按「只固定设计」处理：设计四档保留、差异登记于此。见 `IMPLEMENTATION_STATUS.md` F8/F9。

- [ ] **PA-52（R2）`linkas` 有语法承诺、无实现（2026-09-21 登记）**：`linkas` 在 BNF §1.2 属 40 个语法关键字之一，`token.h:135` 有 `TOK_LINKAS`，但 PA `parser.c` 对该 token **零消费**——写了不报错也不生效。设计本轮保留该记号（模块系统属 2.0 承诺）。动作：实现或在前端即时报「未实现」，二者择一，不得继续静默接受。

- [ ] **PA-53（R16）已移除记号的 lexer 死 token 在 1.x 保持冻结（2026-09-21 登记，决策：不摘）**：`TOK_FAT_ARROW`（`=>`）、`TOK_SAFE_ASSIGN`（`?=`）、`TOK_SAFE_DOT`（`?.`）仍被词法器识别，语法层已按 PA-13/PA-20/PA-21 显式拒绝并给出改写法提示。ltree 口径：设计文档已不描述它们，lexer 摘除留给各线自主，**1.0 冻结线不动词法**（避免影响 `IMPLEMENTATION_STATUS.md` 的行号引用与既有 err 用例）；2.0 线随 PB 回灌一并摘除（见 `TODO-PB.md`）。
